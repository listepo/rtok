# T82: Windows runners check out with `core.autocrlf=true`; goldens, fixtures and the skill
# files are compared byte for byte, so every text file is LF in the working tree everywhere.
* text=auto eol=lf
# Upstream licence text, stored with its original CRLF.
crates/rtok-webui/ui/fonts/OFL.txt -text
