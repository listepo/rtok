/target
.serena/
.reasonix/

# Hugo docs site (site/)
/site/public/
/site/resources/
/site/_vendor/
/site/.hugo_build.lock
report/jscpd-report.json

# Agent worktrees must not be indexed by graph walk
.worktrees/
.claude/
# T184: rtok's own home, and a stray tracked copy of it (1a40127)
/.rtok/
# A config path left as a literal `~` resolves against the cwd (T169)
/~/
rtok.db
rtok.db-shm
rtok.db-wal
/.commandcode

# T183: tools/publish_marketplace's own test runs
__pycache__/
.pytest_cache/
