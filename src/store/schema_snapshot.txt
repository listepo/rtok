index|archive_decisions_archive|archive_decisions|CREATE INDEX archive_decisions_archive ON archive_decisions(archive_id)
index|calls_kind|calls|CREATE INDEX calls_kind ON calls (kind, ts)
index|calls_parent|calls|CREATE INDEX calls_parent ON calls (parent_id)
index|calls_plugin|calls|CREATE INDEX calls_plugin ON calls (plugin, ts)
index|calls_session|calls|CREATE INDEX calls_session ON calls (session_id, ts)
index|events_session|events|CREATE INDEX events_session ON events (session, ts)
index|logs_session|logs|CREATE INDEX logs_session ON logs (session, ts)
index|logs_source|logs|CREATE INDEX logs_source ON logs (source, name, ts)
index|logs_ts|logs|CREATE INDEX logs_ts ON logs (ts)
index|measurements_call|measurements|CREATE INDEX measurements_call ON measurements (call_id)
index|measurements_once|measurements|CREATE UNIQUE INDEX measurements_once ON measurements (once_key)
index|measurements_plugin|measurements|CREATE INDEX measurements_plugin ON measurements (plugin, ts)
index|measurements_session_ts|measurements|CREATE INDEX measurements_session_ts ON measurements (session, ts)
index|note_embeddings_model|note_embeddings|CREATE INDEX note_embeddings_model ON note_embeddings (model)
index|notes_topic|notes|CREATE UNIQUE INDEX notes_topic ON notes (COALESCE(project, ''), kind, title)
index|sqlite_autoindex___diesel_schema_migrations_1|__diesel_schema_migrations|
index|sqlite_autoindex_archive_1|archive|
index|sqlite_autoindex_archive_decisions_1|archive_decisions|
index|sqlite_autoindex_extractor_1|extractor|
index|sqlite_autoindex_hosts_1|hosts|
index|sqlite_autoindex_kv_1|kv|
index|sqlite_autoindex_models_1|models|
index|sqlite_autoindex_otel_export_1|otel_export|
index|sqlite_autoindex_providers_1|providers|
index|sqlite_autoindex_read_cache_1|read_cache|
index|sqlite_autoindex_sessions_1|sessions|
index|sqlite_autoindex_symbol_stale_1|symbol_stale|
index|symbols_root_name|symbols|CREATE INDEX symbols_root_name ON symbols(root, name)
index|symbols_root_path|symbols|CREATE INDEX symbols_root_path ON symbols(root, path)
index|tokens_call|tokens|CREATE INDEX tokens_call ON tokens (call_id, phase)
index|tokens_plugin|tokens|CREATE INDEX tokens_plugin ON tokens (plugin, ts)
index|usage_call|usage|CREATE INDEX usage_call ON usage (call_id)
index|usage_session|usage|CREATE INDEX usage_session ON usage (session, ts)
table|__diesel_schema_migrations|__diesel_schema_migrations|CREATE TABLE __diesel_schema_migrations ( version VARCHAR(50) PRIMARY KEY NOT NULL, run_on TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP )
table|archive|archive|CREATE TABLE archive ( id TEXT PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), session TEXT NOT NULL, tool TEXT, bytes INTEGER NOT NULL, path TEXT NOT NULL, sha256 TEXT NOT NULL , agent_id TEXT NULL)
table|archive_decisions|archive_decisions|CREATE TABLE "archive_decisions" ( session TEXT NOT NULL, tool_use_id TEXT NOT NULL, archive_id TEXT NOT NULL REFERENCES archive(id), pointer TEXT NOT NULL, expanded_ts INTEGER, ts INTEGER NOT NULL DEFAULT (unixepoch()), PRIMARY KEY (session, tool_use_id) )
table|call_io|call_io|CREATE TABLE call_io ( call_id INTEGER PRIMARY KEY REFERENCES calls(id), request_bytes INTEGER NOT NULL DEFAULT 0, response_bytes INTEGER NOT NULL DEFAULT 0, request_sha256 TEXT, response_sha256 TEXT, request_json TEXT, response_json TEXT, request_archive TEXT REFERENCES archive(id), response_archive TEXT REFERENCES archive(id) , request_raw BLOB NULL, response_raw BLOB NULL)
table|calls|calls|CREATE TABLE calls ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), session_id TEXT NOT NULL REFERENCES sessions(id), host_id INTEGER REFERENCES hosts(id), provider_id INTEGER REFERENCES providers(id), model_id INTEGER REFERENCES models(id), plugin TEXT, surface TEXT NOT NULL, kind TEXT NOT NULL, parent_id INTEGER REFERENCES calls(id), name TEXT, ms REAL, ok INTEGER NOT NULL DEFAULT 1, error TEXT )
table|events|events|CREATE TABLE events ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), session TEXT NOT NULL, event TEXT NOT NULL, tool TEXT, plugin TEXT, ms REAL )
table|extractor|extractor|CREATE TABLE extractor ( root TEXT PRIMARY KEY NOT NULL, fingerprint TEXT NOT NULL , indexed_at INTEGER)
table|hosts|hosts|CREATE TABLE hosts ( id INTEGER PRIMARY KEY, slug TEXT NOT NULL UNIQUE, kind TEXT NOT NULL, created_at INTEGER NOT NULL DEFAULT (unixepoch()) )
table|kv|kv|CREATE TABLE kv ( key TEXT PRIMARY KEY NOT NULL, value TEXT NOT NULL )
table|logs|logs|CREATE TABLE logs ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), level TEXT NOT NULL, source TEXT NOT NULL, name TEXT NOT NULL, session TEXT, call_id INTEGER REFERENCES calls(id), plugin TEXT, message TEXT NOT NULL, fields TEXT )
table|measurements|measurements|CREATE TABLE measurements ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), session TEXT NOT NULL, plugin TEXT NOT NULL, kind TEXT NOT NULL, before_bytes INTEGER NOT NULL, after_bytes INTEGER NOT NULL, est_before INTEGER NOT NULL, est_after INTEGER NOT NULL, ref_id TEXT , call_id INTEGER REFERENCES calls(id), once_key TEXT)
table|models|models|CREATE TABLE models ( id INTEGER PRIMARY KEY, provider_id INTEGER NOT NULL REFERENCES providers(id), slug TEXT NOT NULL, created_at INTEGER NOT NULL DEFAULT (unixepoch()), UNIQUE (provider_id, slug) )
table|note_embeddings|note_embeddings|CREATE TABLE note_embeddings ( note_id INTEGER PRIMARY KEY NOT NULL REFERENCES notes(id) ON DELETE CASCADE, model TEXT NOT NULL, dims INTEGER NOT NULL, text_hash TEXT NOT NULL, embedded_at INTEGER NOT NULL DEFAULT (unixepoch()), vector BLOB NOT NULL )
table|notes|notes|CREATE TABLE notes ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), project TEXT, kind TEXT NOT NULL, title TEXT NOT NULL, body TEXT NOT NULL , retired INTEGER NULL, superseded_by INTEGER NULL, pinned INTEGER NOT NULL DEFAULT 0, uses INTEGER NOT NULL DEFAULT 0, last_used INTEGER NULL)
table|notes_fts|notes_fts|CREATE VIRTUAL TABLE notes_fts USING fts5 (title, body, content = 'notes', content_rowid = 'id')
table|notes_fts_config|notes_fts_config|CREATE TABLE 'notes_fts_config'(k PRIMARY KEY, v) WITHOUT ROWID
table|notes_fts_data|notes_fts_data|CREATE TABLE 'notes_fts_data'(id INTEGER PRIMARY KEY, block BLOB)
table|notes_fts_docsize|notes_fts_docsize|CREATE TABLE 'notes_fts_docsize'(id INTEGER PRIMARY KEY, sz BLOB)
table|notes_fts_idx|notes_fts_idx|CREATE TABLE 'notes_fts_idx'(segid, term, pgno, PRIMARY KEY(segid, term)) WITHOUT ROWID
table|otel_export|otel_export|CREATE TABLE otel_export ( stream TEXT PRIMARY KEY, mark BIGINT NOT NULL DEFAULT 0 )
table|providers|providers|CREATE TABLE providers ( id INTEGER PRIMARY KEY, slug TEXT NOT NULL UNIQUE, name TEXT NOT NULL, created_at INTEGER NOT NULL DEFAULT (unixepoch()) )
table|read_cache|read_cache|CREATE TABLE read_cache ( session TEXT NOT NULL, path TEXT NOT NULL, sha256 TEXT NOT NULL, ts INTEGER NOT NULL DEFAULT (unixepoch()), archive_id TEXT, PRIMARY KEY (session, path) )
table|sessions|sessions|CREATE TABLE sessions ( id TEXT PRIMARY KEY, host_id INTEGER REFERENCES hosts(id), project TEXT, cwd TEXT, source TEXT, started_at INTEGER NOT NULL DEFAULT (unixepoch()), ended_at INTEGER )
table|symbol_stale|symbol_stale|CREATE TABLE symbol_stale ( root TEXT NOT NULL, path TEXT NOT NULL, PRIMARY KEY (root, path) )
table|symbols|symbols|CREATE TABLE symbols ( id INTEGER PRIMARY KEY, path TEXT NOT NULL, name TEXT NOT NULL, kind TEXT NOT NULL, line INTEGER NOT NULL, is_def INTEGER NOT NULL, file_sha TEXT NOT NULL , root TEXT NOT NULL DEFAULT '', mtime BIGINT NOT NULL DEFAULT 0, size BIGINT NOT NULL DEFAULT 0, end_line INTEGER NOT NULL DEFAULT 0, scope TEXT NOT NULL DEFAULT '')
table|tokens|tokens|CREATE TABLE tokens ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), call_id INTEGER NOT NULL REFERENCES calls(id), plugin TEXT, phase TEXT NOT NULL, source TEXT NOT NULL, tokens INTEGER NOT NULL, bytes INTEGER, input INTEGER, output INTEGER, cache_create INTEGER, cache_read INTEGER )
table|usage|usage|CREATE TABLE usage ( id INTEGER PRIMARY KEY, ts INTEGER NOT NULL DEFAULT (unixepoch()), session TEXT NOT NULL, model TEXT, input INTEGER NOT NULL DEFAULT 0, cache_create INTEGER NOT NULL DEFAULT 0, cache_read INTEGER NOT NULL DEFAULT 0, output INTEGER NOT NULL DEFAULT 0 , call_id INTEGER REFERENCES calls(id), api TEXT NOT NULL DEFAULT 'anthropic')
trigger|notes_ad|notes|CREATE TRIGGER notes_ad AFTER DELETE ON notes BEGIN INSERT INTO notes_fts (notes_fts, rowid, title, body) VALUES ('delete', old.id, old.title, old.body); END
trigger|notes_ai|notes|CREATE TRIGGER notes_ai AFTER INSERT ON notes BEGIN INSERT INTO notes_fts (rowid, title, body) VALUES (new.id, new.title, new.body); END
trigger|notes_au|notes|CREATE TRIGGER notes_au AFTER UPDATE ON notes BEGIN INSERT INTO notes_fts (notes_fts, rowid, title, body) VALUES ('delete', old.id, old.title, old.body); INSERT INTO notes_fts (rowid, title, body) VALUES (new.id, new.title, new.body); END
