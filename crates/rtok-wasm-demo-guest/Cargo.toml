[package]
name = "rtok-wasm-demo-guest"
version = "0.0.0"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib"]

# Demo guest is wasm32-only; never ship via cargo-dist (see precise-builds).
[package.metadata.dist]
dist = false
