<!-- @generated by git-cliff from cliff.toml — DO NOT EDIT. Regenerate: `just changelog`. -->
# Changelog

Generated by git-cliff from commit subjects (`just changelog`; release-plz on a release PR).
Do not edit by hand — change the commit or `cliff.toml` and regenerate.
Unreleased work is listed first; a `v*` tag starts a version section.

## 0.6.2 — 2026-09-21

### Plan
- plan: add T122-T126 from the research/ideas pass (`f42bb6c`)
- plan: claim T122 T123 T125 T126 (`77a92ee`)
- plan: add T127 (Read advice: small ranged native Read is the edit gate) (`cd61724`)
- plan: add T128-T137 (sub-agent context sharing; ideas/research pass), research §17 (#143) (`2462d2a`)

### Other
- Merge pull request #137 from listepo/plan-t122-t126 (`e91ca7b`)
- T127: Read advice: a small ranged native Read is the edit gate (`32fedd3`)
- Merge pull request #140 from listepo/t127-read-advice-gate (`ba52aaf`)
- docs(agents): Haiku for docs/scans/commands only; mid-tier for code (`3a6e8f4`)
- Merge pull request #145 from listepo/docs/agents-model-tiers (`28ea780`)
- docs(agents): raise the per-task file limit from 3 to 10 (`7ffdf16`)
- Merge pull request #146 from listepo/docs/task-files-limit (`031cbce`)
- T123: rtok doctor names [proxy.tools_rewrite] when it applies (`1373ecb`)
- T123: detect the rtok hop by proxy port; fix report test initializers, clippy, fixture (`ee609f1`)
- Merge origin/main into t123; keep both done.md entries (`c614234`)
- Merge pull request #141 from listepo/t123 (`f2f382b`)
- T121: Codex plugin tree (plugins/codex/) (`23892f7`)
- T121: record the live Codex install check (`f36607d`)
- Merge origin/main into t121-codex-plugin; keep both done.md entries (`b60da61`)
- Merge pull request #136 from listepo/t121-codex-plugin (`77ee442`)
- T126: roadmap.md and research.md §16.2 list shipped work as open (`76fb4a3`)
- T126: restore plan.md Reference; replace the stale roadmap lane table (`cf02da5`)
- Merge origin/main into t126; keep T126 and T127 done.md entries (`aeb1ae0`)
- Merge origin/main into t126 after #141 (`c828ca0`)
- Merge pull request #138 from listepo/t126 (`808e6cc`)
- T106: otel/export.rs unit tests (`8881ba9`)
- T106: fix flaky demon upgrade and spinner tests (`42779f7`)
- Merge origin/main into t106-otel-export-tests; keep T106 done entry (`b007e33`)
- Merge origin/main into t106 after #136 (`9997aa1`)
- Merge pull request #133 from listepo/t106-otel-export-tests (`8874eaf`)
- T122: A dedup pointer reaches a context that never saw the body (`e218ae9`)
- T122: key only ranged reads on returned bytes; split sub-agent context to T127 (`4688216`)
- T122: drop the orphaned plan/check lines (`7368cd5`)
- Merge origin/main into t122; keep both done.md entries (`c5400a7`)
- T122: rustfmt read/mod.rs for fmt-check (`35baeb3`)
- Merge origin/main into t122 after #141 (`f98c0af`)
- Merge origin/main into t122 after #138 (`f5aa2fd`)
- Merge pull request #139 from listepo/t122 (`4279909`)
- T127: toon and compress on by default; fix their rewrite bugs (#144) (`58ca4ce`)
- T125: rtok stats: thinking-block share — the gate for I-86 (#142) (`5bf99a5`)
## 0.6.1 — 2026-09-21

### Plan
- plan: T120 clear the CodeQL findings (`8af36af`)

### Docs
- docs: one task = one branch = one PR (D16) (#135) (`903fefc`)

### Tooling
- ci: auto-revert 836b799 — main CI failed (`5495cf3`)
- ci: auto-revert 7a63f2e — main CI failed (`66f8987`)
- ci: pin vite for vitest and harden spinner redraw test (`0e78416`)

### Other
- T113: rtok tui freezes on start and on tab switches (`a07ba0b`)
- Fix CodeRabbit issues in PR #130 (`7b9354f`)
- T92, T103, T104: omp host, store query tests, schema drift guard (#128) (`0057d4e`)
- T112: restart prompt spinner no longer erases the typed answer (#129) (`836b799`)
- T112: reland restart prompt fix, vitest tests and done.md spacing (#132) (`86391c2`)
- Claude Code plugin, --yes install through the claude CLI, vitest for TS plugin tests (#131) (`7a63f2e`)
- merge: origin/main into fix/tui-background-model (`f5a3553`)
- merge: origin/main into fix/tui-background-model (`05b51fe`)
- T111: TS plugin tests on vitest, with snapshots (`ac7e035`)
- T113: rtok tui freezes on start and on tab switches (`fded00f`)
- T114: Claude Code plugin tree (plugins/claude) (`cb2963b`)
- T115: rtok agents install claude --yes installs the plugin through the claude CLI (`1a305ec`)
- T119: CodeQL on GitHub and locally (`21a8928`)
- T120: Clear the CodeQL findings (`89ebe2e`)
- T119, T120: CodeQL on GitHub and locally, findings cleared (#134) (`9f495b8`)
- Merge remote-tracking branch 'origin/main' (`ba4f68e`)
- Merge branch 'main' of github.com:listepo/rtok (`09fc119`)
- merge: origin/main into fix/tui-background-model (`be7644f`)
- fix(windows): put mise npm:vite on NODE_PATH for vitest (`f60f582`)
- ci(windows): skip vitest-driven plugin unit tests (T82) (`705b0d7`)
- Merge pull request #130 from listepo/fix/tui-background-model (`1f02cf9`)
## 0.6.0 — 2026-09-21

### Other
- docs(plugins): root and per-host README/AGENTS guides (#126) (`b8c8a87`)
- T111: Embed the WASM bundle in the binary (#127) (`ca9f691`)
## 0.5.0 — 2026-09-21

### Tasks
- T92.1: the shared pi extension runs correctly under oh my pi (`b369e28`)

### Plan
- plan: T87-T89 Devin host cards and T101-T107 test-coverage cards (`673fd0c`)

### Other
- T84: auto-revert opens a PR that brings the reverted work back (`8d95d51`)
- T85: Kimi Code plugin tree (plugins/kimi/) (`4282a30`)
- T90: Antigravity plugin tree (plugins/antigravity/) (`7df7bda`)
- T92: record the registerTool double-path risk under omp (`05e1384`)
- T97: `rtok agents install kilo` — Kilo Code: the shared OpenCode plugin plus `kilo.json` MCP (`65f2d11`)
- T98: rtok hook reads Grok Build's hook envelope (`d240984`)
- T99: Grok Build plugin tree (plugins/grok/) (`baa42fc`)
- T97: `rtok agents install kilo` — extra tests; plugin links repair a dangling symlink (`30b29a8`)
- T93: rtok overflows the main-thread stack on Windows (`f20c8a7`)
- T82: Windows back in CI: green job with a named exclusion list (`d41cecd`)
- T87 (wip): rtok hook --host devin maps Devin's payload (`a8643ae`)
- T108: guard tests for the Windows CI job (`9a20e37`)
- T109: .editorconfig matches the repository (`2dffa9e`)
- T110: oxlint and oxfmt for the JS/TS files (`38abbe9`)
- Fix pre-merge checks in PR #125 (`464d8ae`)
- T80: demon status names the proxy endpoint (bind:port) (`daa5ae5`)
- T77: ZCode plugin offered on install (--yes), singleton with the config surfaces (#123) (`1b00eee`)
- site: wire Algolia DocSearch, disable FlexSearch (`79e549d`)
- Merge pull request #122 from listepo/feat/algolia-docsearch (`b59f263`)
- merge origin/main into t80; keep T80 and T81 in done.md (`f8dd120`)
- Merge pull request #124 from listepo/t80 (`50a4172`)
- merge origin/main into windows-ci-and-hosts; keep both done.md sides (`f4ba002`)
- Fix Windows CI hang in demon proxy endpoint status test (`6dda5f9`)
- Merge pull request #125 from listepo/windows-ci-and-hosts (`d24f2be`)
## 0.4.0 — 2026-09-21

### Other
- fix: the bump workflow has no dry run (`9c04c1f`)
## 0.3.3 — 2026-09-21

### Tasks
- T70.2: close the pi context archive-live-zone card (#121) (`aeb3ce6`)

### Docs
- docs: default web UI URL in the commands table (`ef2be07`)

### Other
- T77: one descriptor for the host-plugin offer (`6c579c7`)
- T78: host installers against real copies of the machine's own agent configs (`53431db`)
- T80: rtok web from an installed binary 404s the whole UI (`44fe7fe`)
- fix: webui WASM build (`cba11ac`)
- T81: ship the WASM bundle with the release archive (`1c49a61`)
## 0.3.2 — 2026-09-20

### Docs
- docs: T73 upgrade-race evidence (stop-wait incomplete before replace); file T74 load-sensitive gate tests (`2f01e57`)
- docs: T73 upgrade-race evidence; T74 load-sensitive gate tests (tui, trycmd, otel) (`0e261ac`)
- docs: move T73-T75 to done.md with check results (`bf9feea`)

### Tooling
- ci: auto-revert 2f01e57 — main CI failed (`d010ae7`)
- ci: auto-revert cd3873d — main CI failed (`72bc70a`)

### Other
- fix: wildcard the version in trycmd goldens so a release bump can't break the gate; T74 scoped to tui+otel (`c5265e7`)
- Merge pull request #117 from listepo/docs/gate-flake-cards (`1537b6c`)
- T75: agents uninstall leaves green installed check stuck (`b4cf49e`)
- T76: offer host restart after agents install/uninstall (`6cae43d`)
- T76: timed restart prompt defaults to no (`4c8eca1`)
- T76: config restart_prompt_timeout_seconds, default 60s (`a6dcc01`)
- fix: T75 queue the retention purge behind concurrent store writers (`b18c2de`)
- Merge remote-tracking branch 'origin/main' into fix/mcp-database-locked (`397d250`)
- Merge pull request #118 from listepo/fix/mcp-database-locked (`1fe3b02`)
- T76: spell out no-reply=no restart; default 60s (`83d261c`)
- T76: timeout only when config >0; 0 waits forever (`0d4e1da`)
- T76: restore plan.md; timeout only if config >0 (`af7b61e`)
- T76: no default timeout; left CLI spinner acceptance (`cd3873d`)
- T76: offer host restart after agents install/uninstall (`bb1329e`)
- T76: silence needless_return in host restart helpers (`e6467a2`)
- T76: keep path param for Windows/Linux start_desktop (`12068ef`)
- research: §16 token savings beyond shipped AirTalk surface (`769bcad`)
- T73: quiesce demon surfaces between stop and replace; isolate trycmd under load (#119) (`66fdf74`)
- T74: deterministic load-sensitive gate tests (`a8516f7`)
- T75: agents uninstall clears the installed mark over a materialized plugin copy (`3facd4a`)
- Merge pull request #120 from listepo/wt/t73-t74-t75 (`c27496d`)
## 0.3.1 — 2026-09-20

### Tasks
- T57.1: flag-aware guard read-only classes (`2eb40ac`)

### Tooling
- ci: auto-revert 8bc495c — main CI failed (`7a330fe`)

### Other
- design: retina (2×) landing hero and icon assets (#74) (`5af3ca3`)
- merge main into PR #81; resolve conflicts (`92f454e`)
- Merge pull request #81 from listepo/t53.3 (`88fe589`)
- merge main into PR #82; resolve conflicts (`4975083`)
- Merge pull request #82 from listepo/t59.6 (`946c974`)
- merge main into PR #83; resolve conflicts (`e21fa02`)
- Merge pull request #83 from listepo/t58.1 (`63e44aa`)
- merge main into PR #85; resolve conflicts (`17e86d9`)
- Merge pull request #85 from listepo/t58.5 (`5ad6709`)
- Merge remote-tracking branch 'origin/main' into t50.3 (`1b09133`)
- Merge pull request #77 from listepo/t50.3 (`f44c049`)
- Merge remote-tracking branch 'origin/main' into t52.2 (`ae23795`)
- Merge pull request #78 from listepo/t52.2 (`1459c20`)
- Merge remote-tracking branch 'origin/main' into t60.1 (`8e0246e`)
- Merge pull request #86 from listepo/t60.1 (`1abd751`)
- Merge remote-tracking branch 'origin/main' into t59.5 (`4673dbc`)
- Merge pull request #87 from listepo/t59.5 (`deb9167`)
- Merge remote-tracking branch 'origin/main' into t60.3 (`346ce08`)
- Merge pull request #89 from listepo/t60.3 (`e24ef70`)
- Merge remote-tracking branch 'origin/main' into t60.4 (`459ce05`)
- Merge pull request #90 from listepo/t60.4 (`0b71941`)
- Merge remote-tracking branch 'origin/main' into t68.9 (`1f02c2b`)
- Merge pull request #93 from listepo/t68.9 (`623fd15`)
- Merge remote-tracking branch 'origin/main' into t62.3 (`0d8e323`)
- Merge pull request #96 from listepo/t62.3 (`85ecd6f`)
- Merge remote-tracking branch 'origin/main' into t63.1 (`e21bd79`)
- Merge pull request #98 from listepo/t63.1 (`ab8be81`)
- Merge remote-tracking branch 'origin/main' into t69.6 (`d78216d`)
- Merge pull request #99 from listepo/t69.6 (`4840d0a`)
- Merge remote-tracking branch 'origin/main' into t64.1 (`6698f1a`)
- Merge pull request #100 from listepo/t64.1 (`e265742`)
- Revert "Merge pull request #100 from listepo/t64.1" (`72d59af`)
- test: isolate heavy tests via threads-required, pin -j to CPUs (#115) (`8bc495c`)
- fix: un-break main after the #100/#115 revert cascade; land pending session-end work (#116) (`b7d2821`)
## 0.3.0 — 2026-09-20

### Tooling
- deps: bump the minor-and-patch group with 4 updates (`de47408`)
- deps: bump fs4 from 0.13.1 to 1.1.0 (#70) (`83219ab`)
- deps: bump tower-http from 0.6.11 to 0.7.1 (#69) (`402ac7c`)
- deps: bump windows-sys from 0.59.0 to 0.61.2 (#68) (`5dc35a7`)
- deps: bump libsqlite3-sys from 0.30.1 to 0.38.2 (#67) (`3aeb475`)
- ci: run on main pushes and non-draft PRs to main (`734c203`)

### Other
- T72: agents info, _backup folder, uninstall (`b313b29`)
- Prepare release (`1a40127`)
- fix(agents): ship skills/ in release and fall back to pi copy (`85ec9e4`)
- test: bump trycmd version/man fixtures to 0.2.0 (`2ec7e56`)
- Merge pull request #73 from listepo/fix/ship-skills-for-agent-install (`091ce37`)
- Merge pull request #66 from listepo/dependabot/cargo/minor-and-patch-e0f7a0553e (`63f6c10`)
- fix(sys): use std File locks, drop fs4 (`25373f9`)
## 0.2.0 — 2026-09-18

### Tasks
- T44.1: Backup once per content (`89873a1`)
- T45.4: Dead config keys and stale public numbers (`912788b`)
- T44.2: `rtok agents` with one folder per host (`ffe6130`)
- T44.3: Plugins per app and Claude Desktop (`c75e94b`)
- T44.4: Checks, e2e and platforms (`785e005`)
- T44.5: OpenCode CLI+Desktop MCP and plugin parity with Cursor (`dbe6734`)
- T46.1: ZCode host (`4300480`)
- T46.2: Kimi Code host (`dc23120`)
- T46.3: Copilot hook payload map (`bcf7841`)
- T46.4: Copilot CLI and GitHub Copilot app host (`40546c7`)
- T44.6: Host plugin docs links (`a423aca`)
- T47.4: Repair main after the swept snapshot commit (`d20c1f1`)
- T47.1: Rename rtok agents setup to rtok agents install (`c7d82c6`)
- T47.2: Remove and list e2e for every host (`3360c8f`)
- T47.3: Unit and e2e tests for every host plugin (`dda8137`)
- T45.3: Archive decisions scoped per session (`ca6b39e`)
- T45.1: OTel flush survives a traces error (`6aa200a`)
- T45.2: Proxy cache hits and errors keep usage rows (`b32d68d`)
- T45.5: Gates cover tests, webui and examples (`7116cec`)
- T45.6: Extra hook/expand/guard/read/toon coverage (`7c7cfa5`)
- T47.5: Host plugin Node tests on every OS (`f367ae2`)
- T48.1: pi loads the rtok extension from its linked directory (`3567dd5`)
- T48.2: pi install hint reaches the model (`5d3f270`)
- T48.4: DeepSeek Harness host (`77af448`)
- T48.3: Cursor plugin MCP goes through the ketch-hint launcher (`2b8c266`)
- T52.5: Type-position and scoped-call references (`cfbee16`)
- T51.4: rtok wrap -- <agent> (`ef6c6ff`)
- T48.7: aider host (`0dfcad1`)
- T54.1: Agents support table in docs (`bcf7488`)
- T52.4: Dead code report (`e2e43a8`)
- T51.2: Anthropic native context editing (`7d1e2a1`)
- T49.1: rtok stats --price (`ae54cbf`)
- T48.5: Windsurf host (`526869a`)
- T53.2: Shell completions and man page (`4106630`)
- T51.3: Gemini wire in the proxy (`1709ec3`)
- T50.4: Optional deny of native Grep and Glob (`417c36b`)
- T52.1: Query language over the graph index (`9ffb6c3`)
- T50.2: User filter drop-in directory and schema (`9c36d5b`)
- T48.6: Zed host (`533d68a`)
- T55.7: Stats strip_prefix_cd and quoted paths (`f954d60`)
- T55.13: Copilot path-keyed Read events reach guard and read-advice (`9cbeacf`)
- T55.11: expand freezes pointers by archive id, not session (`3e3317b`)
- T55.14: fold tool results, tool_use ids and image hashes into the cache key (`316bd7f`)
- T55.16: guard deny reads archive metadata, not the body (`73b9617`)
- T58.3: Measure the old_string share of assistant output (`2f11253`)
- T58.3: keep stats --json byte-identical when nothing was edited (`9a064c5`)
- T49.2: Ingest Codex session logs as an api row; OpenCode, Cursor, Copilot documented as unsupported (`b98fd5c`)
- T59.4: Lossless MCP wrapper for foreign servers (rtok mcp -- <server argv>) (`fbf010f`)
- T59.4: fake-server tests for both framings, help golden, cmd-rules docs (`290d50b`)
- T59.4: close-out — evidence row, README command row, plan/todo/done/ideas sync (`f47d786`)
- T59.2: Canonicalize cwd once per search / tree call (`11eef8b`)
- T55.10: One cmd_stem shared by formatters, stats and is_rtok_bin (`ca27a09`)
- T55.8: guard owns its read-key invalidation (read<TAB>path scheme) (`ccc44a2`)
- T55.9: guard Bash key keeps the effective cd target (quote-aware fold) (`605623c`)
- T62.2: compaction checkpoint lists the skills loaded so far (`ab0f29f`)
- T62.1: guard config knobs skills / skill_max_bytes (`c2a42ef`)
- T62.1: guard digests oversized skill bodies on PreToolUse(Skill) (`1e513d3`)
- T62.1: Claude Code registers PreToolUse(Skill); guard and agents docs (`d3b3f21`)
- T62.1: nine-entry hook twins; zcode pins its documented entries by name (`9367865`)
- T56.2: Vfs twins for the read re-dedup cache; close the card (`9d46b85`)
- T55.12: nothing with an apostrophe is wrapped on Windows (Git Bash is POSIX) (`8d84aa0`)
- T69.1: note lifecycle — retire, supersede, pin, never delete (`458f36c`)
- T55.15: live_blobs yields text blocks only; image/document payloads stay byte-identical (`e470abd`)
- T68.1: explore answers a code question in one MCP call (`96272ca`)
- T60.10: sessions cursor, live-only filter and scroll on the TUI (`438e404`)
- T65.4: stack-trace blocks survive the head/tail cut (`1d09962`)
- T51.1: compress JSON and code inside the live zone (`e36bc48`)
- T72.1: shorter dev build and test loop (`7ab9d8b`)
- T70.2: pi context carrier for the archive live zone (`d5e7f21`)
- T61.1: stats folds injected skill bodies with a resident column (`e8c569f`)
- T61.3: doctor skills audit with the measured warns (`dcadb6a`)
- T60.8: KEYS table drives every key hint; ? overlay and r refresh (`6c7aa7a`)
- T70.2: fix pi carrier + bless goldens and host table (`4701646`)
- T65.3: collapse_columns folds columnar padding; the default rule turns it on (`61f65b8`)
- T59.7: doctor names the host-native features that duplicate a rtok surface (`1674529`)
- T56.5: close out ReadFs — the walk swap fails its own measured-parity gate (`f9f7854`)
- T64.3: prompt-cache FAQ with the measured hit rates per surface (`869baa7`)
- T67.2: expand --context N prints merged windows around grep hits (`13a6608`)
- T50.1: stats filter column for bash families (`9b48c5a`)
- T50.1: per-family cmd filter rules and docs table (`7b769d8`)
- T50.1: evidence in research.md, close task card (`e6e443a`)
- T50.1: cmd golden fixtures for new filter families (`a67eb58`)
- T58.5: docker ps and kubectl get table formatters (`6160ced`)
- T58.5: ps aux process-table formatter (`7581dec`)
- T58.5: cite table-formatter measurements on cmd docs (`3a32dde`)
- T58.5: close table-formatter task (`cd85508`)
- T64.1: claim grouping pass (`d2a3fae`)
- T64.1: group dir/diag pass in the rule engine (`6cf0d96`)
- T64.1: pin grouped lines and cover dir/diag (`21bc435`)
- T64.1: ls and find use group=dir instead of take(40) (`8bbf320`)
- T64.1: find golden for directory grouping (`0aedb8a`)
- T64.1: rg -l grouping; grep hits stay ungrouped (`a3f3386`)
- T64.1: tsc diagnostics group by TS code (`be7e697`)
- T64.1: tsc_dup golden beats the ungrouped rule (`3a02ced`)
- T64.1: eslint diagnostics group by rule id (`59f2ffa`)
- T64.1: eslint_dup golden beats the ungrouped rule (`022f035`)
- T64.1: dotnet CS codes and cargo E codes group (`875b7a7`)
- T64.1: cargo check golden groups E0308 (`54ba43a`)
- T64.1: dotnet_dup golden groups CS1002 (`4946dfe`)
- T64.1: cite grouping savings; grep stays ungrouped (`c85350d`)
- T64.1: close grouping pass (`360e5a8`)
- T69.6: add [plugins.memory] sync_tokens = 300 (`6256195`)
- T69.6: keep the last-written memory sync block hash in the store (`1727439`)
- T69.6: render a managed rtok:memory block for CLAUDE.md (`e18778e`)
- T69.6: add rtok memory sync CLI (`ad217cd`)
- T69.6: doctor overlap when a sync block and hook recall both run (`887ca3c`)
- T69.6: document memory sync on the plugin page (`16a747f`)
- T69.6: trycmd golden for rtok memory sync --help (`79aeab3`)
- T69.6: complete memory sync in clap goldens and D27 exemptions (`6d51924`)
- T69.6: close memory sync in plan (`8d61c0e`)
- T60.5: web Plugins set goes through config set (`e66e2e5`)
- T60.5: Plugins page toggle sends set over /ws (`633fbd6`)
- T60.5: close plugin toggle on the web Plugins page (`2159ddf`)
- T60.5: collapse inbound send if for clippy (`1595ce2`)
- T60.3: add session_detail model accessor (`9719cb3`)
- T60.3: Enter opens session detail on the TUI (`196b899`)
- T60.3: click opens session detail on the web (`9fde49b`)
- T60.3: pin session detail on both surfaces (`68b1d15`)
- T60.3: close per-session drill-down (`a4a56dc`)
- T60.4: add expand_payload model accessor (`50479fc`)
- T60.4: Enter-key expand pane on the TUI (`c42c724`)
- T60.4: answer expand over inbound WebSocket (`9c06284`)
- T60.4: expand button and filter on the web Calls page (`ece0e0c`)
- T60.4: pin archive expand on both surfaces (`45473dc`)
- T60.4: close archive expand on tui and web (`40614d1`)
- T63.1: claim skills page on tui and web (`71f3773`)
- T63.1: join listing and resident into skills_from (`32f2a48`)
- T63.1: skills tab cursor and never-invoked filter (`e38c6cf`)
- T63.1: render the skills table on the TUI (`636523b`)
- T63.1: skills page and never-invoked checkbox on the web (`c458ec2`)
- T63.1: pin the skills page on both surfaces (`281311c`)
- T63.1: close skills page on tui and web (`4d9c85c`)
- T58.2: count transcript compact_boundary events in rtok stats (`6333958`)
- T58.2: put live archive ids on the compaction checkpoint (`2d08e2a`)
- T58.2: register Cursor preCompact as the compaction checkpoint (`954a612`)
- T58.2: register Copilot preCompact as the compaction checkpoint (`ac50d96`)
- T58.2: register Codex PreCompact and PostCompact hooks (`ac97b9c`)
- T58.2: bless the host table and record compact evidence (`76e1d3c`)
- T58.2: close the compaction-checkpoint card (`11e9b4a`)
- T70.6: claim plugin compaction and record host findings (`ecf5830`)
- T70.6: append OpenCode compaction checkpoint through the plugin (`fa04207`)
- T70.6: save pi compaction without replacing the host summary (`dfbc9bd`)
- T70.6: assert pi and OpenCode compact restore matches Claude Code (`88d9a9b`)
- T70.6: document pi and OpenCode plugin compaction paths (`381b34b`)
- T70.6: close compaction on pi and OpenCode through the plugin (`994e7b6`)
- T57.1: flag-aware guard read-only keys (`82e1a00`)
- T70.5: claim guard-on-plugin and record host block schema (`68e607a`)
- T70.5: rtok guard check wraps plugins::guard (`a59d9a8`)
- T70.5: test guard check allow, deny, and T57.1 false-deny (`2d092a8`)
- T70.5: pi tool_call blocks duplicates through guard check (`8e57587`)
- T70.5: OpenCode before-hook denies through guard check (`0156749`)
- T70.5: document plugin guard check on pi and OpenCode (`6eb6470`)
- T70.5: note reasoned block APIs on pi and OpenCode (`f894e48`)
- T70.5: bless host table with guard on pi and OpenCode (`6bd9d18`)
- T70.5: snapshot rtok guard check in help and completions (`5c4e108`)
- T70.5: close guard on pi and OpenCode through the plugin (`b7e4255`)
- T70.3: claim pi registerTool path and record step 1 (`500747e`)
- T70.3: default-off setup.pi.tools config key (`92d59c4`)
- T70.3: snapshot setup.pi.tools in config show (`eef14f3`)
- T70.3: rtok mcp --call shims the listed MCP tools (`a5d88af`)
- T70.3: register measured tools through pi.registerTool (`3cddca1`)
- T70.3: mark MCP plugins reached on pi via registerTool (`1acd252`)
- T70.3: note registerTool on the pi package and reaches test (`3b31428`)
- T70.3: bless host table with read, memory, graph on pi (`a92d6eb`)
- T70.3: snapshot mcp --call in bash completions (`5e82f78`)
- T70.3: close pi registerTool tools on the plugin (`5b7eda7`)
- T62.3: archive filter stdin on the run path (`8de3c70`)
- T62.3: bless filter --archive bash completions (`92b9865`)
- T62.3: add [skill] rule and Measurement kind (`83cb35a`)
- T62.3: shorten OpenCode skill bodies after execute (`9b90194`)
- T62.3: close OpenCode skill filter on the tool path (`a2ae7b1`)
- T68.9: claim with/without graph bench (`d4a5e0e`)
- T68.9: graph suite questions and mini repos (`226dcdd`)
- T68.9: add bench.suite config key (`8234392`)
- T68.9: rtok bench --suite graph flag (`cd7c678`)
- T68.9: graph suite dry-run and zero table (`0c44725`)
- T68.9: live graph_one via claude -p and stats prices (`09f892a`)
- T68.9: graph suite unit tests for dry-run and table (`d494184`)
- T68.9: trycmd dry-run for bench --suite graph (`678e237`)
- T68.9: complete --suite on bash completions (`9fb5b46`)
- T68.9: document graph suite and dry-run; live still open (`de01f9b`)
- T68.9: close graph with/without bench (live API still open) (`fd95dce`)
- T61.2: add SkillRef to the plugin wire (`c347eef`)
- T61.2: yield SkillRef after a Launching skill result (`384ec5f`)
- T61.2: archive skill bodies outside keep_turns (`faad0e1`)
- T61.2: document the archive skills off switch (`fd654b6`)
- T61.2: replay a 3-turn skill body through the proxy (`3c80a22`)
- T61.2: move the card from plan to done (`e31fb2c`)
- T59.5: record MCP description-token share (`71dea07`)
- T59.5: add proxy.tools_rewrite config (`7417e9b`)
- T59.5: rewrite tools[] descriptions in the proxy (`fae54d2`)
- T59.5: test Anthropic and OpenAI Chat tools[] rewrite (`8d11f5c`)
- T59.5: proxy wire tests and config-show golden (`ebc1895`)
- T59.5: move the card from plan to done (`4727891`)
- T60.1: emit --json from the operator model on table readers (`75e330c`)
- T60.1: trycmd golden for plugins --json (`2e33009`)
- T60.1: trycmd golden for agents sessions --json (`755f185`)
- T60.1: trycmd golden for logs --json (`3567e7d`)
- T60.1: trycmd golden for otel status --json (`3e8617c`)
- T60.1: trycmd golden for demon status --json (`f7db589`)
- T60.1: trycmd golden for agents list --json (`ed3757f`)
- T60.1: trycmd golden for doctor --json (`6ad5d61`)
- T60.1: complete --json on reading commands (`b4d5b43`)
- T60.1: gate --json on reading commands (`a250621`)
- T60.1: close --json on every reading command (`ac757ac`)
- T68.5: walk changed-file defs to reachable tests (`4d6e2e9`)
- T68.5: fixture tests for affected test selection (`a1a56d3`)
- T68.5: rtok graph affected CLI (`36c346e`)
- T68.5: impact path-only MCP and graph docs (`b2ed14d`)
- T68.5: close the affected-tests task (`9511423`)
- T68.6: capture import tags for use, import and require (`c29b2b7`)
- T68.6: store import rows without counting them as refs (`af78af0`)
- T68.6: follow import edges in impact_bfs (`36caad6`)
- T68.6: record this-repo index time before and after (`b594e32`)
- T68.6: close the import-edges task (`71e6ed5`)
- T52.3: add graph.map_tokens, off by default (`e637491`)
- T52.3: show map_tokens in config-show golden (`eb878f3`)
- T52.3: query top-referenced symbols byte-stably (`bcbbe27`)
- T52.3: inject a ranked repo map at SessionStart (`add5d32`)
- T52.3: close the ranked repo-map task (`028dee4`)
- T52.2: claim the extra-grammar task (`f2a1e67`)
- T52.2: add optional Java Kotlin Swift C# Ruby PHP grammars (`884e981`)
- T52.2: tag Java Kotlin Swift C# Ruby PHP outlines (`9e6fff4`)
- T52.2: fingerprint and watch the extra grammars (`5d49568`)
- T52.2: rustfmt the extra outline match arms (`ecd26ef`)
- T52.2: record index db size; skip payload compression (`7aed418`)
- T52.2: close the extra-grammar task (`b681c03`)
- T58.1: count changed re-reads in stats (`12e7bc3`)
- T58.1: add read.delta and read.delta_max_ratio (`0222b63`)
- T58.1: return a unified diff on changed re-reads (`4d905af`)
- T58.1: advise mode=diff after Edit of a cached file (`9d17541`)
- T58.1: rustfmt the delta Vfs tests (`ef317f1`)
- T58.1: close the card after the 7.3% gate (`e06f6ac`)
- T58.1: collapse the delta-advice if for clippy (`419f5f2`)
- T58.1: rustfmt the delta-advice let-chain (`2ea5fa1`)
- T65.1: count same-session content-hash repeats in stats (`c4290e8`)
- T65.1: look up same-session archive rows by content hash (`06ff15a`)
- T65.1: return a pointer for identical cmd and read output (`c85b3ab`)
- T65.1: close the card after the 1.9% gate (`5715365`)
- T50.3: add comments-stripped read mode (`1c0d93d`)
- T50.3: record stripped vs imports measurement (`f066591`)
- T50.3: close extra read modes (`653788c`)
- T59.6: split Agent/Task inputs and results in stats (`74d6295`)
- T59.6: record Agent/Task share under the 5% gate (`7f5de80`)
- T59.6: close — sub-agents are 0.7% of tool-result tokens (`e8afc8f`)
- T53.3: record webpki TLS decision and hook p95 (`9be22b6`)
- T53.3: close hook start without Security.framework (`621e69d`)
- T57.1: flag-aware guard read-only classes (`6893d30`)
- T59.5: byte-stable proxy tools[] description rewrite (`a4b7bd9`)
- T48.8: add the VS Code Copilot Chat host module (`b07df74`)
- T48.8: add [setup.vscode] profile dirs (`3ea217c`)
- T48.8: cover vscode install paths and the host table (`5ad9441`)
- T64.1: group cmd output by directory or diagnostic (`62e50ab`)
- T65.2: compact JSON Bash output before the line cut (`82f6905`)
- T68.5: list tests a change touches (`f41fb7a`)
- T52.3: ranked repo map at SessionStart (`b2f129b`)
- T59.6: keep sub-agent handoff off under the 5% gate (`8688586`)
- T69.2: add optional recall scoring columns (`201704b`)
- T69.6: sync a managed memory block into agent docs (`86895c9`)
- T69.3: seed a no-LLM memory recall bench (`9d39c42`)
- T68.9: add the graph with/without bench suite (`d669877`)
- T69.6: add [plugins.memory] sync_tokens = 300 (`1ba6904`)
- T69.6: keep the last-written memory sync block hash in the store (`ca8c434`)
- T69.6: restore store.rs and register the kv table (`f91bb78`)
- T69.6: render a managed rtok:memory block for CLAUDE.md (`ea0c710`)
- T69.6: add rtok memory sync CLI (`3a2cb31`)
- T69.6: doctor overlap when a sync block and hook recall both run (`2462589`)
- T69.6: document memory sync on the plugin page (`c3c321c`)
- T69.6: trycmd golden for rtok memory sync --help (`4b8b52b`)
- T69.6: complete memory sync in clap goldens and D27 exemptions (`ccf3391`)
- T69.6: close memory sync in plan (`edc7602`)
- T69.3: add planted-fact memory recall bench (`8f07495`)
- T69.3: record memory recall numbers in research §14 (`1698a1e`)
- T69.3: cite memory recall bench from research §14 (`a0b45ec`)
- T69.3: close memory recall bench (`f332266`)
- T69.3: drop unrelated paths from the close commit (`c58794e`)
- T71.3: claim one hub skill per host (`8953c2f`)
- T71.3: share SkillCopy plan with Vfs tests (`85984b6`)
- T71.3: Vfs tests for hub skill install (`dc7fce8`)
- T71.3: list hub skill in doctor and record description bytes (`33cb576`)
- T71.3: close rtok hub skill install (`9038732`)
- T71.3: drop hub skill from the roadmap (`9be3e2a`)
- T53.3: record webpki TLS decision and hook p95 (`ad1401e`)
- T53.3: close hook start without Security.framework (`120d1f0`)
- T69.2: record live-note counts against recall_titles (`eb5f361`)
- T69.2: close — no project exceeds recall_titles (`b639f17`)
- T61.2: archive skill bodies outside the live zone (`9eda39d`)
- T50.1: stats filter column for bash families (`5c8d493`)
- T50.1: per-family cmd filter rules and docs table (`6563bf0`)
- T50.1: evidence in research.md, close task card (`721e652`)
- T50.1: cmd golden fixtures for new filter families (`1bc4ba6`)
- T58.5: docker ps and kubectl get table formatters (`859cc1e`)
- T58.5: ps aux process-table formatter (`fe1abb8`)
- T58.5: cite table-formatter measurements on cmd docs (`b906224`)
- T58.5: close table-formatter task (`45f6637`)
- T64.1: group dir/diag pass in the rule engine (`ca0cbde`)
- T64.1: pin grouped lines and cover dir/diag (`82fe03c`)
- T64.1: ls and find use group=dir instead of take(40) (`3e75dfd`)
- T64.1: find golden for directory grouping (`2735c80`)
- T64.1: rg -l grouping; grep hits stay ungrouped (`2ec96c7`)
- T64.1: tsc diagnostics group by TS code (`400ccd8`)
- T64.1: tsc_dup golden beats the ungrouped rule (`e1f0071`)
- T64.1: eslint diagnostics group by rule id (`424d844`)
- T64.1: eslint_dup golden beats the ungrouped rule (`c8c275c`)
- T64.1: dotnet CS codes and cargo E codes group (`85d979a`)
- T64.1: cargo check golden groups E0308 (`9c7fb29`)
- T64.1: dotnet_dup golden groups CS1002 (`1278fe5`)
- T64.1: cite grouping savings; grep stays ungrouped (`8151dec`)
- T64.1: close grouping pass (`3f7a934`)
- T65.2: compact JSON cmd output before the line cut (`2a98693`)
- T65.2: skip table formatters when the body is JSON (`35750de`)
- T65.2: add gh and aws JSON golden inputs (`ce47cf1`)
- T65.2: add aws and kubectl JSON golden outputs (`7164aa1`)
- T65.2: document JSON cmd compaction and the Bash share (`bc841c8`)
- T65.2: close JSON output compaction (`521ee81`)
- T71.1: park curl HTML formatter below 1% gate (`8fe29fe`)
- T58.1: count changed re-reads in stats (`1833325`)
- T58.1: add read.delta and read.delta_max_ratio (`c519f6a`)
- T58.1: return a unified diff on changed re-reads (`2e8bd2c`)
- T58.1: advise mode=diff after Edit of a cached file (`b1e6cae`)
- T58.1: rustfmt the delta Vfs tests (`cb31106`)
- T58.1: close the card after the 7.3% gate (`905692b`)
- T58.1: collapse the delta-advice if for clippy (`0ad4151`)
- T58.1: rustfmt the delta-advice let-chain (`2aeacec`)
- T65.1: count same-session content-hash repeats in stats (`4ae58a5`)
- T65.1: look up same-session archive rows by content hash (`90a33da`)
- T65.1: return a pointer for identical cmd and read output (`e0b8ce6`)
- T65.1: close the card after the 1.9% gate (`2ea204f`)
- T50.3: add comments-stripped read mode (`554eff7`)
- T50.3: record stripped vs imports measurement (`242042c`)
- T50.3: close extra read modes (`fef6c2f`)
- T68.5: walk changed-file defs to reachable tests (`050149a`)
- T68.5: fixture tests for affected test selection (`999c944`)
- T68.5: rtok graph affected CLI (`4b5d99b`)
- T68.5: impact path-only MCP and graph docs (`b34ab1d`)
- T68.5: close the affected-tests task (`e087ad6`)
- T70.4: shorten Cursor-launched MCP results (`fb6b603`)
- T68.6: capture import tags for use, import and require (`9cab337`)
- T68.6: store import rows without counting them as refs (`3da3b70`)
- T68.6: follow import edges in impact_bfs (`5303075`)
- T68.6: record this-repo index time before and after (`4fb9b8a`)
- T68.6: close the import-edges task (`c6ef917`)
- T52.3: add graph.map_tokens, off by default (`c87a96c`)
- T52.3: show map_tokens in config-show golden (`91ae3f8`)
- T52.3: query top-referenced symbols byte-stably (`29bf38e`)
- T52.3: inject a ranked repo map at SessionStart (`b1631e9`)
- T52.3: close the ranked repo-map task (`4ba415a`)
- T52.2: add optional Java Kotlin Swift C# Ruby PHP grammars (`7c6c258`)
- T52.2: fingerprint and watch the extra grammars (`fd4fe1b`)
- T52.2: record index db size; skip payload compression (`3927aaf`)
- T52.2: close the extra-grammar task (`47d286c`)
- T58.2: put archive ids on the compaction checkpoint (`7902769`)
- T71.2: SessionEnd checkpoint restored at next startup (`b24024e`)
- T59.6: off-by-default handoff MCP tool for sub-agents (`a24dd57`)
- T68.9: graph suite questions and mini repos (`30bcf3b`)
- T68.9: add bench.suite config key (`9507d81`)
- T68.9: rtok bench --suite graph flag (`5ebbc5f`)
- T68.9: graph suite dry-run and zero table (`2335e56`)
- T68.9: live graph_one via claude -p and stats prices (`c2f57a1`)
- T68.9: graph suite unit tests for dry-run and table (`8dce7f7`)
- T68.9: trycmd dry-run for bench --suite graph (`9a00c49`)
- T68.9: complete --suite on bash completions (`46d29cd`)
- T68.9: document graph suite and dry-run; live still open (`a796ef7`)
- T68.9: close graph with/without bench (live API still open) (`7833e56`)
- T60.5: web Plugins set goes through config set (`e0d55eb`)
- T60.5: Plugins page toggle sends set over /ws (`7c934d5`)
- T60.5: close plugin toggle on the web Plugins page (`98d9e54`)
- T60.5: collapse inbound send if for clippy (`ccb0970`)
- T60.3: add session_detail model accessor (`55cbf20`)
- T60.3: Enter opens session detail on the TUI (`d98cffe`)
- T60.3: click opens session detail on the web (`b350072`)
- T60.3: pin session detail on both surfaces (`ef0166b`)
- T60.3: close per-session drill-down (`fd17da9`)
- T60.4: add expand_payload model accessor (`aeb5688`)
- T60.4: Enter-key expand pane on the TUI (`b8eb8ce`)
- T60.4: answer expand over inbound WebSocket (`055c021`)
- T60.4: expand button and filter on the web Calls page (`66bc5e2`)
- T60.4: pin archive expand on both surfaces (`328dc2e`)
- T60.4: close archive expand on tui and web (`fa771a1`)
- T63.1: claim skills page on tui and web (`4990a07`)
- T63.1: join listing and resident into skills_from (`8aed763`)
- T63.1: skills tab cursor and never-invoked filter (`df32686`)
- T63.1: render the skills table on the TUI (`fc7721d`)
- T63.1: skills page and never-invoked checkbox on the web (`1221093`)
- T63.1: pin the skills page on both surfaces (`4a418a6`)
- T63.1: close skills page on tui and web (`3cdd10f`)
- T60.1: emit --json from the operator model on table readers (`df7c5bd`)
- T60.1: trycmd golden for plugins --json (`f02c4d5`)
- T60.1: trycmd golden for agents sessions --json (`21e1567`)
- T60.1: trycmd golden for logs --json (`f87ef3c`)
- T60.1: trycmd golden for otel status --json (`e20be23`)
- T60.1: trycmd golden for demon status --json (`715a8c0`)
- T60.1: trycmd golden for agents list --json (`8803dd1`)
- T60.1: trycmd golden for doctor --json (`058097d`)
- T60.1: complete --json on reading commands (`09fc81d`)
- T60.1: gate --json on reading commands (`f21e2d9`)
- T60.1: close --json on every reading command (`119163d`)
- T60.2: claim trycmd goldens for every subcommand (`8b73995`)
- T60.2: trycmd --help goldens for every subcommand (`d5dd4ac`)
- T60.2: trycmd goldens for stats table, stats --json, info --json (`5bb9107`)
- T60.2: trycmd goldens for doctor, plugins, config path (`67c8442`)
- T60.2: trycmd goldens for config get, validate, set (`b4c334e`)
- T60.2: trycmd golden for config init --dry-run (`4bc66b4`)
- T60.2: trycmd golden for zsh completions (`145731f`)
- T60.2: trycmd golden for fish completions (`acce14a`)
- T60.2: trycmd golden for powershell completions (`35620ef`)
- T60.2: trycmd golden for man page (`a10c3b9`)
- T60.2: trycmd goldens for agents list, sessions, demon status (`6af69a0`)
- T60.2: trycmd goldens for otel status, logs, report --format md (`e820a5e`)
- T60.2: trycmd golden for proxy --dry-run and case index (`18df1a9`)
- T60.2: trycmd goldens for hook, mcp tools/list, filter --cmd (`9e04229`)
- T60.2: trycmd goldens for run -- echo and expand --lines --grep (`cd1dffe`)
- T60.2: close trycmd goldens for every subcommand (`ff4cb84`)
- T70.1: archive and measure `rtok filter` stdin (`4571c93`)
- T70.1: shorten pi read/grep/find/ls tool results (`d8cc049`)
- T70.1: document pi file/search filter path (`dbc20a5`)
- T70.1: close pi file/search tool_result filter (`c5c0009`)
- T58.2: count transcript compact_boundary events in rtok stats (`5194ab5`)
- T58.2: put live archive ids on the compaction checkpoint (`e557e28`)
- T58.2: register Cursor preCompact as the compaction checkpoint (`57fb691`)
- T58.2: register Copilot preCompact as the compaction checkpoint (`ff2c01e`)
- T58.2: register Codex PreCompact and PostCompact hooks (`b7cb51d`)
- T58.2: bless the host table and record compact evidence (`ed6ff72`)
- T58.2: close the compaction-checkpoint card (`f3859e8`)
- T70.6: claim plugin compaction and record host findings (`d1a84ef`)
- T70.6: append OpenCode compaction checkpoint through the plugin (`5fe015f`)
- T70.6: save pi compaction without replacing the host summary (`04a94fd`)
- T70.6: assert pi and OpenCode compact restore matches Claude Code (`8e6a35c`)
- T70.6: document pi and OpenCode plugin compaction paths (`62cd660`)
- T70.6: close compaction on pi and OpenCode through the plugin (`07504c3`)
- T70.5: claim guard-on-plugin and record host block schema (`2c29055`)
- T70.5: rtok guard check wraps plugins::guard (`8976d56`)
- T70.5: test guard check allow, deny, and T57.1 false-deny (`32955f6`)
- T70.5: pi tool_call blocks duplicates through guard check (`402de8e`)
- T70.5: OpenCode before-hook denies through guard check (`b7ae82c`)
- T70.5: document plugin guard check on pi and OpenCode (`7f6de85`)
- T70.5: note reasoned block APIs on pi and OpenCode (`2118089`)
- T70.5: bless host table with guard on pi and OpenCode (`df013b7`)
- T70.5: snapshot rtok guard check in help and completions (`ccd7e9e`)
- T70.5: close guard on pi and OpenCode through the plugin (`192bf7b`)
- T70.3: claim pi registerTool path and record step 1 (`d42800a`)
- T70.3: default-off setup.pi.tools config key (`9c0ce67`)
- T70.3: snapshot setup.pi.tools in config show (`b0837d9`)
- T70.3: rtok mcp --call shims the listed MCP tools (`3138ac1`)
- T70.3: register measured tools through pi.registerTool (`fd52738`)
- T70.3: mark MCP plugins reached on pi via registerTool (`234d83f`)
- T70.3: note registerTool on the pi package and reaches test (`7788a3c`)
- T70.3: bless host table with read, memory, graph on pi (`ce45347`)
- T70.3: snapshot mcp --call in bash completions (`3af1ee4`)
- T70.3: close pi registerTool tools on the plugin (`5138a1c`)
- T62.3: archive filter stdin on the run path (`add8245`)
- T62.3: bless filter --archive bash completions (`3a3e64d`)
- T62.3: add [skill] rule and Measurement kind (`6fde49f`)
- T62.3: shorten OpenCode skill bodies after execute (`3ab3796`)
- T62.3: close OpenCode skill filter on the tool path (`f77b4ba`)
- T59.6: split Agent/Task inputs and results in stats (`6344fd4`)
- T59.6: record Agent/Task share under the 5% gate (`01cde0a`)
- T59.6: close — sub-agents are 0.7% of tool-result tokens (`c3c39c1`)
- T71.2: count sessions with and without a checkpoint note in rtok stats (`54a8088`)
- T71.2: show checkpoint coverage on the stats table and JSON (`ba8d52b`)
- T71.2: add plugins.memory.startup_recall, off by default (`b7e4946`)
- T71.2: bless config-show for startup_recall (`2e96634`)
- T71.2: save a session checkpoint on SessionEnd and restore it at startup (`13e0439`)
- T71.2: record session-checkpoint coverage in research.md (`29c6e75`)
- T71.2: close the session-handoff card (`4409b71`)
- T70.4: claim Cursor MCP result hook and record verified schema (`b26cb58`)
- T70.4: share wrap shorten_result for MCP content blocks (`4144178`)
- T70.4: map Cursor postToolUse MCP output onto PostToolUse (`26d380c`)
- T70.4: shorten Cursor MCP results through the wrap path (`95afafc`)
- T70.4: register postToolUse for host-launched MCP results (`6ebfb48`)
- T70.4: document Cursor postToolUse MCP result replacement (`9671bf1`)
- T70.4: e2e hook shortens long MCP results and skips rtok (`3d1a0bf`)
- T70.4: close Cursor MCP result hook (`cce66e5`)
- T48.8: document vscode host and bless agents table (`a0e4369`)
- T48.8: name vscode in CLI help and README (`278407f`)
- T48.8: close VS Code Copilot Chat host (`1714753`)
- T48.8: drop vscode from the roadmap open-row list (`9638946`)
- T53.1: add outline-first to the opt-in nudge set (`deff12a`)
- T53.1: record SessionStart bytes and dry A/B zeros (`4932fb6`)
- T53.1: close coaching nudges under an A/B (`317d112`)
- T53.1: drop nudges from the inject roadmap lane (`02b9792`)
- T53.1: record live nudge A/B (`35dc584`)
- T59.5: record MCP description-token share (`6c90cdd`)
- T59.5: add proxy.tools_rewrite config (`d02cfe2`)
- T59.5: rewrite tools[] descriptions in the proxy (`d67c69b`)
- T59.5: test Anthropic and OpenAI Chat tools[] rewrite (`558fc52`)
- T59.5: proxy wire tests and config-show golden (`f1c6e4f`)
- T59.5: move the card from plan to done (`857db7e`)
- T70.4: keep all Cursor hook events in the host README (`ab876aa`)

### Plan
- plan: promote I-38 to T57.1 (flag-aware guard read-only classes) (`5e123f1`)
- plan: gap tasks per weakness — T50.1 re-scoped, T58.2 re-scoped to hosts + archive ids, T58.5, research §9 corrections (`a5436fa`)
- plan: promote I-30/I-39/I-40/I-44–I-48 to T59.1–T59.8; claim T49.2, T58.3, T58.4, T59.4 (`95e7bbf`)
- plan: drop T55.10 and T59.2 from todo.md after done (`4f9d18a`)
- plan: T60.1–T60.9 — CLI, TUI and web UI improvements from the 2026-09-17 surface survey (`6f8bd6d`)
- plan: T60.2 re-scoped — trycmd goldens for every subcommand, not only --help (`4f942ae`)
- plan: T60.10 — Sessions tab in the TUI is a static paragraph (cursor, scroll, live filter) (`81a8ab5`)
- plan: T61.1–T61.3 — measure, archive and audit injected skill bodies; research §10.7; I-49/I-50/I-51 promoted (`12a9218`)
- plan: T62.1–T62.3 — host-plugin side of the skill-body blind spot; research §10.8 (`5fff67a`)
- plan: claim T62.1, T62.2 (`0aff5a2`)
- plan: T63.1 — skills page on tui and web (used vs never, body bytes, resident share) (`ec59e1f`)
- plan: close T62.2 (`5fd0d32`)
- plan: T64.1–T64.3, T65.1–T65.4 — rtk's four strategies and sqz against rtok; research §11; comparison (`627b169`)
- plan: restore T66.1/T66.2 (memory), renumber the expand tasks to T67.1/T67.2 (`3e3fa91`)
- plan: close T67.1 (`c1184cf`)
- plan: T68–T71 cards, idea renumbering, stale claims reset (`b188b4a`)
- plan: record T51.1 remaining proxy-acceptance step (WIP claim already in tree) (`11b1fbb`)
- plan: close T62.1 (`cd2c262`)
- plan: close T56.3 (`62e4a0f`)
- plan: close T68.1 (`454e4a5`)
- plan: remove the T56.5 card (close-out recorded in f9f7854) (`327ad78`)
- plan: close T64.3 (prompt-cache FAQ) (`a2a0b81`)

### Docs
- docs: record 2026-09-17 bug-hunt findings as T55 (`a641a23`)
- docs: mark T55.1–T55.6 done in the 2026-09-17 review notes (`fbb0643`)
- docs: note testing library candidates (2026-09-17) (`046b14e`)

### Tooling
- ci: auto-revert c75e94b — main CI failed (`39ae7c3`)
- ci: auto-revert ef6c6ff — main CI failed (`c217b8f`)
- ci: comment out advisory Windows job (hanging nextest) (`224b215`)
- ci: auto-revert b188b4a — main CI failed (`8ab8d9a`)
- chore: pin Colima stack in mise and document for agents (#71) (`cd5b867`)
- chore: pin Colima stack in mise and document for agents (#71) (`50ee09b`)
- ci: auto-revert 6c7aa7a — main CI failed (`cd2b94f`)
- ci: disable workflow on push to main (#72) (`efa4479`)
- ci: gate pull requests again (`647939f`)

### Other
- fix: expand PowerShell-style ~\ paths in config (`7fa1fb3`)
- Merge pull request #37 from listepo/fix/crossplat-agent-bugs (`aa4651f`)
- Revert "Merge pull request #37 from listepo/fix/crossplat-agent-bugs" (`e5001ce`)
- fix: restore ~\ expand, match Bin store, bump trycmd (`42752c3`)
- Merge pull request #38 from listepo/fix/crossplat-agent-bugs-2 (`0cee3ce`)
- fix: Windows doctor MCP spawn, skill home, live ring preference (`6ae3d7c`)
- Merge pull request #39 from listepo/fix/crossplat-agent-bugs-3 (`a490c04`)
- fix: keep Windows drive in read normalize and LSP file URIs (`7647ee3`)
- Merge pull request #40 from listepo/fix/crossplat-agent-bugs-4 (`f10a1ac`)
- fix: absolute rtok on Windows PATH miss; case-safe read under (`4ac282d`)
- Merge pull request #41 from listepo/fix/crossplat-agent-bugs-5 (`3e06c1e`)
- fix: quote absolute rtok hook paths that contain spaces (`b94d660`)
- Merge pull request #42 from listepo/fix/crossplat-agent-bugs-5b (`717dd15`)
- fix: clear leftover Cursor mcp.json while plugin is linked (`68f45f9`)
- Merge pull request #43 from listepo/fix/crossplat-agent-bugs-7 (`f0fffdc`)
- fix: use cmd/PowerShell for rtok run on native Windows (`f135ca3`)
- Merge pull request #44 from listepo/fix/crossplat-agent-bugs-8 (`1398349`)
- fix: replace read-only Windows configs in agent atomic write (`58e5856`)
- Merge pull request #45 from listepo/fix/crossplat-agent-bugs-9 (`4910dba`)
- fix: Windows cmd stems and search display paths (`dbd9b03`)
- Merge pull request #46 from listepo/fix/crossplat-agent-bugs-10 (`a8f54b8`)
- fix: mixed-case rtok.exe stems and strict expand ranges (`e67c482`)
- Merge pull request #47 from listepo/fix/crossplat-agent-bugs-11 (`bc6f4bc`)
- T48: Promote open ideas to plan (`32c099a`)
- Merge origin/main: keep T44.3 over the CI auto-revert (`a684d98`)
- Merge pull request #48 from listepo/docs/review-bugs-plan (`de32baa`)
- Merge local T48–T53 completions into main. (`b0c653b`)
- fix: set UsageFields.container on Anthropic and OpenAI wires (`8ed0735`)
- T51.1 T53.1 T53.3: live blobs, nudges, and webpki TLS (`d899760`)
- Revert "Merge pull request #48 from listepo/docs/review-bugs-plan" (`1a11fbd`)
- Merge origin/main: apply T55 plan revert. (`2d277a5`)
- fix: T55.1–T55.6 Windows/agent bugs + test VFS (`d7b54fa`)
- Revert "Merge origin/main: apply T55 plan revert." (`0ac6a07`)
- merge: origin/main into fix/t55-windows-bugs (`c97ebd5`)
- Merge pull request #49 from listepo/fix/t55-windows-bugs (`c41e4f0`)
- Merge pull request #50 from listepo/docs/t55-review-status (`d2dcc4c`)
- Revert "Merge pull request #50 from listepo/docs/t55-review-status" (`d2637a3`)
- fix: agents list test on clean CI; note T55.1–6 done (`662da14`)
- Merge pull request #51 from listepo/fix/ci-agents-list-plugins (`ccd6c61`)
- Revert "Merge pull request #51 from listepo/fix/ci-agents-list-plugins" (`db0f93b`)
- refactor: T56 Vfs helper + migrate read/search/cmd rules tests (`5647156`)
- fix: bench kill, archive live_blobs fixture, agents list plugins (`f951a9c`)
- Merge pull request #53 from listepo/refactor/t56-vfs-migrate (`7c06dca`)
- Revert "Merge pull request #53 from listepo/refactor/t56-vfs-migrate" (`ddc5bf8`)
- refactor: T56 Vfs helper + migrate read/search/cmd rules tests (`e481e5a`)
- fix: bench kill, archive live_blobs fixture, agents list plugins (`5827dce`)
- fix: agents remove_twice tolerates idle sibling variants (`b57d632`)
- Merge pull request #55 from listepo/refactor/t56-vfs-migrate-2 (`fd698c0`)
- Revert "Merge pull request #55 from listepo/refactor/t56-vfs-migrate-2" (`8764fbc`)
- refactor: T56 Vfs helper + migrate read/search/cmd rules tests (`9684280`)
- fix: bench kill, archive live_blobs fixture, agents list plugins (`603331c`)
- fix: agents remove_twice tolerates idle sibling variants (`874308b`)
- style: rustfmt agents_install remove_twice helpers (`3aa1502`)
- Merge pull request #56 from listepo/refactor/t56-vfs-migrate-3 (`36871ce`)
- Revert "Merge pull request #56 from listepo/refactor/t56-vfs-migrate-3" (`97cca5d`)
- refactor: T56 Vfs helper + migrate read/search/cmd rules tests (`70cfc44`)
- fix: bench kill, archive live_blobs fixture, agents list plugins (`9684616`)
- fix: agents remove_twice tolerates idle sibling variants (`5acd0b2`)
- style: rustfmt agents_install remove_twice helpers (`958641f`)
- test: restore disk Settings::load twins; add more Vfs coverage (`cf38fda`)
- fix: drop wrap from surface_parity EXEMPT until command exists (`63c92ef`)
- Merge pull request #57 from listepo/refactor/t56-vfs-restore-2 (`d73ba72`)
- test: T56.2/3 WalkBuilder + agent setup Vfs twins (`971fac8`)
- Merge pull request #59 from listepo/feat/t56-walkbuilder-agent-vfs (`f7ce40a`)
- feat: T56.4 WalkFs adapter for search/tree on Vfs (`685729d`)
- Merge pull request #60 from listepo/feat/t56-4-vfs-walk-adapter (`0cd8ebb`)
- Revert "Merge pull request #60 from listepo/feat/t56-4-vfs-walk-adapter" (`a81fd79`)
- feat: T56.4 WalkFs adapter for search/tree on Vfs (`b6eeb0c`)
- Merge pull request #61 from listepo/feat/t56-4-vfs-walk-adapter-2 (`d88ba42`)
- feat: T56.5 ReadFs trait for read() on Vfs (`8c4accf`)
- Merge pull request #62 from listepo/feat/read-fs-trait-vfs (`a5b0969`)
- fix: stub wasm host imports for native guest tests (#63) (`ec559c4`)
- fix(ci): terminate hung Windows nextest, cap job at 30m (#64) (`552ad01`)
- Merge pull request #58 from listepo/docs/testing-lib-candidates (`b242ffc`)
- Improve docs: grammar, clarity, and getting-started guides. (`e186f1e`)
- Merge pull request #54 from listepo/docs/copy-pass (`625b2f1`)
- Merge pull request #65 from listepo/feat/disable-windows-ci (`1f0a8a1`)
- T55.11-T55.16: second review pass — six bug cards, I-39/I-40 (docs only) (`56715e1`)
- research: competitive gap review 2026-09-17 (§9), I-41–I-48, T58.1–T58.4 (`f627d1f`)
- research: §10 skill loading cost across hosts, measured 2026-09-17; ideas I-49–I-52 (`c3d2d57`)
- T67.1 T67.2: mem_save upserts on project+kind+title; rtok memory export (`545b622`)
- T66.1 T66.2: renumber the memory tasks, close them in the plan (`f0600da`)
- fix: graph_lsp_gate counts the explore tool (T68.1 gate miss) (`2612c53`)
- Merge branch 'main' of github.com:listepo/rtok (`5aaa240`)
- Merge main of github.com:listepo/rtok: keep T60.8/T61.3 (CI red was T70.2's, since fixed in 4701646); take the workflow disable (`68502cb`)
- Close plan complexity-2 tasks: cmd, report, graph, memory, agents, web. (`45c4531`)
- Close complexity-2 tasks in plan; fix webui WASM build. (`0731efd`)
- Repair main: restore a green `just check` (`8c81ecc`)
- man golden: wildcard the git sha (`f8dcbf2`)
- store: serialize migrations across processes (`5e11123`)
- otel: measure the unreachable endpoint against a baseline, not a clock (`f18be5b`)
## 0.1.6 — 2026-09-16

### Tooling
- chore: pin cargo-nextest in mise for CI just check (`5af2dac`)

### Other
- refactor: use dunce/pathdiff for graph paths; prefer nextest (`2022fd0`)
- Merge pull request #36 from listepo/refactor/path-crates-nextest (`1074083`)
- fix: resolve host plugins from ketch store when bin has none (`fd657f2`)
- style: rustfmt resolve_plugin_src call sites (`719986d`)
- test: bump trycmd --version snapshot to 0.1.5 (`caca69d`)
- Merge pull request #35 from listepo/fix/ketch-store-plugin-src (`cec7040`)
## 0.1.5 — 2026-09-16

### Other
- fix: Windows plugin setup via copy and ship plugins in dist (`f4676f1`)
- fix: collapse plugin_src if-lets for clippy (`1fec07c`)
- test: bump trycmd --version snapshot to 0.1.4 (`2b64a72`)
- Merge pull request #34 from listepo/fix/windows-plugin-setup (`64db9d1`)
## 0.1.4 — 2026-09-16

### Other
- fix: resolve user home via USERPROFILE on Windows (#33) (`a025b77`)
## 0.1.3 — 2026-09-15

### Tooling
- ci: auto-revert c5ebcbd — main CI failed (`d0ae3e9`)

### Other
- ci(release): faster cuts + artifact sizes (#30) (`82f8cd2`)
- Merge pull request #31 from listepo/release/v0.1.3 (`32d0b4e`)
- fix: Windows compile for release dist (#32) (`71d5c77`)
## 0.1.2 — 2026-09-15

### Other
- fix: keep demon status columns aligned under colour (`9e1442b`)
- Merge pull request #27 from listepo/fix/demon-status-table (`631bff0`)
- fix: accept legacy plugins.read.languages for compat (`7482db7`)
- Merge pull request #28 from listepo/fix/read-languages-compat (`5ca8fa0`)
## 0.1.1 — 2026-09-15

### Tasks
- T35.1: compile each tags query once (`de41fbf`)
- T18.7: dependency updates by Dependabot (`2e03746`)
- T35.2: parse on worker threads, write from one (`0150a3d`)
- T18.7: tree-sitter crates reach their own group (`befe31a`)
- T35.2: read tests never move the process cwd (`d7aa422`)
- T28.0: survey LLM compression against lossless archive (`248d528`)
- T31.0: survey semantic cache with a zero false-hit gate (`7e4697d`)
- T33.0: survey L0/L1/L2 tiers and call out AGPL (`f2f0d36`)
- T35.4: the watcher indexes what changed (`d50d971`)
- T29.0: survey embeddings beside FTS5 (`d144abb`)
- T30.0: survey LSP behind the existing tags MCP names (`ce7eb70`)
- T32.0: survey a WASM host that never runs on the hook path (`5db6ba9`)
- T35.5: rebuild a root when the extractor fingerprint changes (`a93c3b1`)
- T36.19: refuse to overwrite setup backups (`c6a5780`)
- T36.5: include archive marker in read cap (`747722b`)
- T36.1: escape control chars in toon cells (`e4a19f9`)
- T36.3: honour cmd rules and fail_tail_lines (`9cddedd`)
- T36.12: otel exporter skip traces without losing streams (`faf5faf`)
- T36.16: graph reads each file once (`5b4ff3e`)
- T36.14: label passthrough rows as bytes not tokens (`3d49cee`)
- T36.4: count every dropped line in cmd rules trailer (`d343b9c`)
- T36.2: persist toon live-zone decision and original bytes (`af7eec2`)
- T36.17: drop index rows when a directory is removed (`56218d3`)
- T36.7: honour --config on every config subcommand (`6ff13d5`)
- T36.15: render instruction audit on web Doctor page (`2c0068e`)
- T36.20: keep archive session and hash stored bytes (`3088cd8`)
- T36.9: expand tilde for every config path key (`901d02f`)
- T36.13: record PDF section page after pagination (`ce96d55`)
- T36.10: apply expand.max_lines and mcp.max_result_chars (`7afbe34`)
- T36.11: run retain_calls_days on proxy and mcp (`a9dc5a2`)
- T36.8: legacy config keys cannot outrank env or flags (`693ea98`)
- T36.6: drop unread plugins.read.languages (`9dc333c`)
- T36.18: wire provider/api constants and Url join (`03942bd`)
- T28.1: config flag for optional compress (default off) (`d80e1a4`)
- T29.1: config flag for optional embed search (FTS5 default) (`ea940bc`)
- T30.1: config flag for optional graph LSP backend (tags default) (`e9e8101`)
- T31.1: opt-in semantic cache config knobs (default off) (`2af52d6`)
- T32.1: config flag for WASM plugin host (default off) (`c6059bc`)
- T33.1: config flag for optional archive tiers (default off) (`10b37d2`)
- T35.3: batch graph index store I/O (`eb92c9d`)
- T33.2: implement optional L0/L1/L2 loading (`95164f9`)
- T31.2: implement opt-in semantic cache (`bf03832`)
- T32.2: implement WASM host + example Measurement (`6220707`)
- T29.2: implement optional embed search (`eb99c07`)
- T28.2: implement optional compress / memory extractor (`0042a11`)
- T30.2: implement optional LSP graph backend (`988bb11`)
- T37.0: Agent list plus CLI/GUI setup variants (`3d9754a`)
- T37.1: E2E tests for the Slint web UI (`e5695ee`)
- T38.1: e2e for uncovered commands (`3fbc50c`)
- T38.4: drop stale divan/lbug rows from toolchain.md (`24507bd`)
- T38.5: commands_e2e.rs on assert_cmd (`7660ef0`)
- T38.6: translate all .md to English (`644f475`)
- T38.2: e2e plugin matrix (`c8d52da`)
- T38.3: e2e memory import + graph index (`8875c61`)

### Plan
- plan: record the fixed bug round and open P36 for its residue (`ec45743`)

### Tooling
- chore: ignore nested agent worktrees (`3cb4bed`)
- ci: bump the actions group across 1 directory with 4 updates (#10) (`a9869bc`)
- chore: pause rtok-plugin-sdk crates.io publish (#20) (`9e41eac`)
- deps: bump toml_edit from 0.22.27 to 0.25.13+spec-1.1.0 (#14) (`ca85dbc`)
- deps: bump the tree-sitter group across 1 directory with 4 updates (#16) (`d2c6947`)
- deps: bump sha2 from 0.10.9 to 0.11.0 (#15) (`e7841b0`)
- deps: bump rmcp from 0.8.5 to 3.2.0 (#11) (`3313b99`)
- ci: faster PR checks (ubuntu-only + skip cache save) (#25) (`8770238`)
- deps: bump similar from 2.7.0 to 3.2.0 (#12) (`186f34f`)
- ci: run only on main/manual; auto-revert failed main pushes (`4cc8f68`)
- ci: drop pull_request trigger (main + manual only) (`a0f6963`)

### Other
- fix: keep hook and expand paths lossless and fail-open (`d8b7cff`)
- fix: build the graph-lbug feature again (`1afd465`)
- fix: install claude hooks into a settings file that is not shaped as expected (`23c8bc2`)
- fix: stop the ultra compressor cutting words in half (`bf53cb6`)
- fix: the plan-file gate reads the catalogue, not a frozen count (`1ff9467`)
- fix: the cmd family matches the command the hook actually wraps (`3ab67fc`)
- fix: git diff keeps the changes and git status keeps the untracked paths (`487a0bd`)
- fix: cmd archives the command's bytes and always names them (`7803a3c`)
- fix: keep proxy usage capture readable, switchable and un-double-counted (`0406f0d`)
- fix: migrate atomically and search notes for the words the user typed (`9858846`)
- fix: config set walks a scalar path instead of panicking on it (`ed77df9`)
- fix: name the MCP server rtok and give the test host real content ids (`ea256d6`)
- fix: read every transcript line and refuse an impossible --since (`56cee14`)
- fix: a live-zone pointer belongs to the session that made it (`2bfd338`)
- fix: bench honours its per-run timeout (`12b1a44`)
- fix: the read advice fails open on a hook body the store elided (`7698f22`)
- fix: keep a name whole, a long stream open and a report escaped (`3180376`)
- fmt: rustfmt after P36 landings (`03e5940`)
- clippy: collapse nested ifs in config and expand (`c806c64`)
- clippy: const thread-local counter in graph symbol cache (`c6b359b`)
- test: pin RTOK_HOME in filter integration test (`2840892`)
- retention: do not purge plugin archives (`5cf2d3a`)
- design(site): Bitset B landing with hero product shots (`f9991f4`)
- design(site): feature-card Signal icons + landing copy (`4e17a88`)
- design(webui): Bitset B RtokTheme restyle for all pages (`4c0a7db`)
- design(webui): bundle IBM Plex Mono for Slint (`d9296b5`)
- design: responsive Bitset B from mobile through TV OLED (`c4d5ad2`)
- Merge pull request #17 from listepo/design/bitset-b-landing (`8cfcff7`)
- design(site): Nebula WebGL hero background (`a2f573f`)
- Merge pull request #19 from listepo/design/landing-nebula-a (`5665ec8`)
- Revert "Merge pull request #19 from listepo/design/landing-nebula-a" (`4517f7b`)
- P39: freeze new LadybugDB work; survey a replacement (`38b9d93`)
- P39: remove LadybugDB and Grafeo; SQLite-only graph symbols (`ba915d3`)
- Merge pull request #23 from listepo/chore/remove-graph-lbug-grafeo (`95c7913`)
- fmt: rustfmt archive, embed, semantic cache, and p29 tests (`da27a38`)
- fix: host clippy/test for no_std wasm guest (#24) (`a8d6d71`)
- tests: divan graph benches, trycmd CLI fixtures, compress snapshot (`eeb7b2f`)
- fix: precise-builds so dist skips no_std wasm guest (#26) (`679473f`)
- T40: Drop demon list and demon update (`226fa7f`)
- fix: precise-builds so dist skips no_std wasm guest (`15d3602`)
- fix: use POSIX octal printf escapes in non-UTF8 archive test (`d55a395`)
- fix: bump trycmd --version fixture to 0.1.1 (`450481c`)
- fix: pin the clock in the backup collision test (`ef78b03`)
- fix: review fixes; T42 module status in agent setup and doctor (`1d15e24`)
- T41.1, T41.2: Dart LSP backend and setup page (`d4d7666`)
- T43: rtok info: version, config, store and proxy in one place (`c5ae9db`)
- Merge branch fix/dist-precise-builds-wasm-guest: T41.1/T41.2, T42 fixes, T43 (`dbcc7a1`)
## 0.1.0 — 2026-09-10

### Tasks
- T19.1: dashboard host and port CLI (`d70ac7c`)
- T19.2: WebSocket dashboard snapshot (`22a833d`)
- T19.3: Slint WASM dashboard UI crate (`8a0f814`)
- T8.13: impact in one query (`9bcdaf2`)
- T8.14: P8c measurement (`9f492a5`)
- T10.5: Cursor plugin offer (`ec2df7e`)
- T8.15: auto_index and watch keys (`7579330`)
- T8.16: watcher in rtok mcp (notify) (`e785c41`)
- T8.17: watchman backend (`b6b75c2`)
- T10.6: pi host plugin (`357708f`)
- T4.6: implement edit-window check in Read-advice hook (`4fd6dbe`)
- T18.6: gate the release on a green test suite (`c63d546`)
- T18.6: match the squash merge GitHub actually writes (`ba9acf5`)
- T18.6: bring the release runbook up to what the workflows do (`3b35266`)
- T18.6: record that the release PR cannot raise the version (`9915fe1`)
- T18.6: stop the otel flake from gating releases (`163a7a0`)
- T18.6: record the measurement behind the otel wait (`ae8ac10`)
- T18.6: comment out the Homebrew release (`4d3c40b`)
- T10.8: move the host installers under `rtok agent setup` (`76d6048`)
- T10.9: rtok agent remove <host>, and a backup before either command (`3c92489`)
- T12.6: --dry-run on the write commands, with a git-shaped coloured diff (`dace210`)
- T20.1: rtok demon — supervise proxy, mcp and dashboard (`eee2f49`)
- T20.2: owo-colors owns the colour question (`2175a90`)
- T21.3: rtok dashboard becomes rtok web; D23 pairs it with rtok tui (`700507e`)
- T15.0: one operator model behind rtok web and rtok tui (`fb85c90`)
- T23.0: where the rtok-plugin-sdk crate line goes (`f0040f4`)
- T23.1: rtok-plugin-sdk owns the contract's value types (`35f3458`)
- T23.2: manifest and dashboard_page are required (`961987b`)
- T23.3: the host moves behind capability traits (`68fc91c`)
- T24.0: [log] section and a sink that rotates (`7b26038`)
- T26.0: just dup — jscpd joins the gate (`1bd9829`)
- T23.3: the Plugin trait moves to the SDK (`2670d7f`)
- T23.4: the ten plugins import from rtok-plugin-sdk (`f25805b`)
- T23.5: the SDK documents itself, with a host to test against (`9367583`)
- T23.6: the release publishes rtok-plugin-sdk (`ddda5d0`)
- T24.2: rtok logs and rtok logs export (`cf0c3e6`)
- T8.18: the watcher tests stop racing the filesystem (`ea4ae14`)
- T24.4: the demon's own logs are bounded too (`a133f2f`)
- T26.1: retire what the duplication gate found (`a26359e`)
- T25.0: a session knows whose it is (`c1f7ad8`)
- T27.0: the crate exists and the five hosts move onto it (`20bf6ff`)
- T8.19: graph_truth is green again and names its numbers (`c959b2f`)
- T24.1: every log line goes through the funnel (`0f558d9`)
- T15.10: the two surfaces cannot drift (`b806e42`)
- T22.0: pick the PDF renderer against the size gate (`66f5b77`)
- T15.11: the model covers every reading command (`f12b955`)
- T25.1: one reader, in the model (`689f196`)
- T22.1: --format md (`f82a71f`)
- T24.3: rtok logs watch (`5b140fb`)
- T15.1: ratatui + crossterm scaffold, event loop (`1241472`)
- T15.2: header · tabs · footer shell (`666ceb9`)
- T15.12: the parity test enumerates commands, not pages (`91b79d4`)
- T10.10: remove residue — flag help, docs rows (`ae9de10`)
- T25.2: rtok agent sessions (`dfc9b4a`)
- T25.3: rtok agent sessions watch (`322faa5`)
- T22.2: --format html (`1246857`)
- T22.4: --ai (`b7d4c2e`)
- T15.8: CLI + [tui] config (`e7d66ee`)
- T15.3: Overview tab (`bcc5da0`)
- T22.3: --format pdf (`ba4c90a`)
- T22.5: recommendations (`9d7fc20`)
- T15.6: Doctor tab (`94903ab`)
- T15.9: TTY guard (`47f67f3`)
- T15.7: Logs tab (`b865f89`)
- T15.4: Plugins tab (`555aa84`)
- T15.5: Calls tab (`34747a7`)
- T19.4: WASM webui renders every model::pages() entry (`b2aafb5`)
- T22.3: the P17 size gate holds (`929fe3f`)
- T34.1: store: concurrent writers, atomic purge, literal read-cache prefix (`d546546`)
- T34.2: latency gates use nearest-rank p95 (`1228729`)
- T34.3: one test fixture instead of eleven (`8582476`)
- T34.4: report: one bar scale, UTF-8-safe PDF text (`bc55131`)
- T34.5: agent-sdk writes host configs atomically (`09152dd`)
- T34.6: proxy: bounded tee, one usage reader (`2325c10`)
- T34.7: otel.headers never prints (`5f7a2a4`)
- T34.8: small bugs behind duplicated helpers (`16ae9af`)
- T34.9: graph tests that name their failure (`c016bec`)

### Plan
- plan: complexity ratings for the remaining work (`cda5d70`)
- plan: T10.6 pi host plugin (`7141d42`)
- plan: Gate P19 passed (dashboard verified over HTTP+WS) (`97f6b29`)
- plan: Gate P8d passed (p95 serialized 8.25ms, all five clauses) (`202c13e`)
- plan: remove gates only traffic/user action can close (`089fe81`)
- plan: mark P8d/P19/T8.17 done and retire code-gated table (`d5f0d5a`)
- plan: record T18.6 and the v0.0.1 release (`8ff413f`)
- plan: D24 and P22 — rtok report in md, html, pdf and --ai (`cd274d6`)
- plan: D25 and P23 — rtok-plugin-sdk, the published plugin contract (`ad8b235`)
- plan: §5 lists every task with status and complexity (`d707452`)
- plan: D26 and P24 — rtok logs over a bounded, rotating file (`3351c70`)
- plan: D27 and P25 — agent sessions, and one model behind every reading command (`a0b39f7`)
- plan: T8.18 — the watcher tests stop racing the filesystem (`3b36587`)
- plan: T8.19 opened (graph_truth is red), and the parallel round recorded (`5cf4f46`)
- plan: P27 (rtok-agent-sdk) recorded — D28 and T27.0 (`9f0aa65`)
- plan: T8.19, T15.11, T27.0 claimed for a parallel round (`93f73dc`)
- plan: T8.19, T15.11, T27.0 done — the second parallel round (`eed5155`)
- plan: third parallel round claimed — T22.1, T24.3, T25.1, T25.2, T15.12, T15.1+T15.2 (`139d870`)
- plan: third parallel round done — T22.1, T24.3, T25.1, T25.2, T15.12, T15.1, T15.2 (`a054922`)
- plan: fourth round claims on main — T22.5 and T15.6 heaps adopted, four TUI tasks (`df68bf4`)
- plan: drop stale §5 task-count header (`bde2a87`)
- plan: count T10.7 done via T10.9, drop superseded bucket (`4c1827b`)
- plan: residual bug-hunt debt as tasks (`f15f111`)
- plan: mark T16.9 T22.6 T24.5 done (`5640b52`)
- plan: promote Later v0.2+ into P28–P33 tasks (`eb5680a`)
- plan: mark T10.11 and T11.8 done (`e308d1e`)
- plan: P34 done (T34.1–T34.9), P35 graph index speed (`f4dccf8`)

### Docs
- docs: sync plan status with shipped tasks (`062e848`)
- docs: align logging, Cursor hooks, OTel flush, webui thinness with residual debt (`2538b1c`)
- docs: git-cliff writes an @generated header into every changelog (`20c94dd`)
- docs: agents never sign commits, merges or PRs (`5966177`)

### Tooling
- ci: Node 26.8.2 and cancel in-progress runs (`fe1d261`)
- ci: sign macOS releases with MACOS_* secrets (`6055448`)

### Other
- T21.1 T21.2: the plugin offer asks, and graph index shows progress (`5d0aea2`)
- T27.0 (wip): rtok-agent-sdk crate, five hosts route through it (`5975877`)
- roadmap: fourth parallel round claims T15.4, T15.5, T15.7, T15.9 (`8ff01b2`)
- gitignore: ignore accidental ./~ temp home (`d3aae00`)
- fix: guard Measurement records avoided archive size (`c032461`)
- fix: cmd trailing-& skip and archive fail-open print (`cd37a2f`)
- fix: read path suffix false positive and symlink escape (`f52165c`)
- fix: proxy usage int_field accepts whole-number JSON floats (`655299d`)
- fix: skip inject apply when no plugin offered context (`76de4dd`)
- merge: hot-path bugfix from bughunt (`ea4305b`)
- fix: cache doctor probes across snapshot ticks (`3ad3b79`)
- fix: document expand.max_rate; stop TUI placeholder lying (`b587826`)
- merge: report/TUI bugfix (doctor cache, expand.max_rate docs) (`cec289c`)
- bugfix: keep session attribution across upserts (`aa9c629`)
- bugfix: agent remove must not wipe a foreign plugin dir (`3305ffa`)
- bugfix: expand bare ~, retire demon orphans, render Sessions in tui (`dfe4104`)
- merge: persist/setup bugfix (session host, remove, tilde, demon) (`8d5d89d`)
- fix: idle-hook ignores busy PostToolUse (T22.6) (`31dd8bd`)
- fix: migrate legacy core.log_* into [log] (T24.5) (`3375a15`)
- fix: single-flight otel flush across processes (T16.9) (`ddcb2b6`)
- fix: wire Cursor afterShellExecution to PostToolUse (`4eeb7d5`)
- fix: respect archive keep_turns live zone in toon (`2a47848`)
- proxy: plain passthrough when proxy/core enabled=false (`a27d6c1`)
- style: rustfmt after proxy live-passthrough UI (`29d8a7d`)
- Merge pull request #8 from listepo/docs/t22.3-linked-measurement (`0ece376`)
- feat(modes): enrich terse/yagni and add native compress + YAGNI ladder (`684bf60`)
- docs(modes): WHY comments + fixture comparison vs caveman/ponytail baselines (`2da33af`)
- test(proxy): serialize live-ring asserts to stop Ubuntu flake (`3f86ab1`)
- Generate docstrings for PR #5 (`b412ddd`)
- Fix CodeRabbit issues in PR #5 (`72f60a9`)
- Merge pull request #5 from listepo/feat/modes-cave-pony (`595eef4`)
- Merge remote-tracking branch 'origin/main' into improve/bugs-tests-reuse (`5113fcb`)
- Merge pull request #9 from listepo/improve/bugs-tests-reuse (`1f18feb`)
## 0.0.1 — 2026-09-08

### Tasks
- T0.1: cargo project (`99773f9`)
- T0.2: config + paths (`6ac6896`)
- T0.3: SQLite store (`0e3d020`)
- T0.4: plugin trait + registry (`22029bf`)
- T0.5: token estimator (`95367af`)
- T0.6: hook I/O types (`757300c`)
- T0.7: CI (`77aef3f`)
- T0.8: plugin SDK — one kind, external plugins, simple examples (`c9b6f81`)
- T12.1: typed config schema + embedded reference file (`fb7bb78`)
- T12.2: move config.rs into config/mod.rs (`686b948`)
- T12.2: figment config layering and config show/get (`3e70d06`)
- T2.2: latency harness (`34acb75`)
- T2.3: rtok setup claude (`05d4b51`)
- T2.4: inject plugin budget (`e8364eb`)
- T3.1: rtok run archives command output (`5d0e6c1`)
- T3.2: cmd rule engine (`f38dcee`)
- T3.5: rtok expand (`4829d3f`)
- T3.4: wrap Bash through rtok run (`15ca870`)
- T2.5: PreCompact checkpoint + compact restore (`1bab52c`)
- T2.6: deny duplicate Read/Bash when archived (`be94cef`)
- T3.6: record cmd measurements in stats JSON (`ecc3128`)
- T3.3: family formatters, default rules, goldens (`88091ff`)
- T7.1: inject terse and yagni modes once per session (`8467bc7`)
- T6.1: mem_save, mem_search, mem_get over FTS5 (`9681091`)
- T6.2: SessionStart recall of last 5 note titles (`779fb22`)
- T6.3: import JSONL notes with body-sha dedupe (`3d4b3b5`)
- T7.2: doctor --instructions lists injectors and tokens (`5801769`)
- T4.1: rtok mcp stdio server with expand (`902f7f1`)
- T5.0: httpmock upstream harness for proxy fixtures (`2c0f95b`)
- T9.3: setup claude --replace drops legacy token hooks (`a28af4f`)
- T4.2: MCP read with numbered lines, cap, root guard (`de6da24`)
- T4.5: search and tree over gitignored files (`71b6423`)
- T4.6: deny native Read of large files not being edited (`df515db`)
- T4.7: register rtok mcp in setup claude (`5e8bf44`)
- T4.4: re-read dedup with unchanged-since hits (`84d8340`)
- T9.1: rtok bench dry-run schedule and result table (`39783df`)
- T5.1: passthrough proxy with usage capture (`1810a0a`)
- T4.3: read map/signatures via tree-sitter-tags (`4242b71`)
- T10.1: Cursor hooks.json setup and field mapping (`04dbae8`)
- T9.2: baseline vs rtok bench results (`050071a`)
- T5.2: proxy health, flags, and setup --proxy (`c93ef67`)
- T8.1: tree-sitter-tags symbol index (`e183d83`)
- T9.4: move reduce-token stack into legacy/ (`44300a1`)
- T10.2: OpenCode stdin filter plugin (`9ecdb10`)
- T5.3: live-zone archive rewrite (`eb4e739`)
- T5.4: expand through the proxy (`3d9e6ae`)
- T5.5: cache-health report (`8ddede8`)
- T8.2: MCP tools (`7e2cc40`)
- T10.3: Codex (`987a60c`)
- T10.4: release (`2b9f36f`)
- T12.5: .env files (`6844cf8`)
- T9.5: README (`0289d8d`)
- T11.1: Wire adapter (`9d85591`)
- T11.2: OpenAI Chat Completions wire (`e5b8db2`)
- T11.3: OpenAI Responses wire (`b50826f`)
- T11.4: archive across wires (`254e256`)
- T11.5: setup for OpenAI hosts (`d7ae2a9`)
- T11.6: usage.api + per-API stats (`d49da71`)
- T11.7: toon on Wire tool results (`3a34014`)
- T8.3: per-root symbol index (`b7c4184`)
- T8.4: stat-gated index freshness (`1f9a5dc`)
- T8.8: labelled hit rate (`d82039a`)
- T8.5: call edges (`fa91363`)
- T8.6: symbol returns the definition (`9c042b4`)
- T8.7: impact(name, depth) (`d97d9a6`)
- T8.9: graph contract tests (`fb6b897`)
- T16.1: `[otel]` config (`6aee322`)
- T16.2: export watermark and row readers (`dd1eb4f`)
- T16.3: OTLP/HTTP JSON encoder (`d629de6`)
- T16.4: ledger to GenAI semconv mapping (`1ccf9ba`)
- T16.5: exporter and `rtok otel` (`4111a9f`)
- T16.6: triggers off the hook path (`ee9ce57`)
- T16.7: cumulative sums (`19e3b3b`)
- T16.8: docs and Gate P16 measurements (`764af4c`)
- T8.10: symbol store seam (`cc85ee0`)
- T8.11: lbug store: open and index writes (`f2d9ba7`)
- T8.12: lbug store: reads (`444e872`)
- T17.1: dev, release and dist profiles (`1e2177b`)
- T18.1: version 0.0.1 and a dispatchable release (`067a257`)
- T18.2: the next patch, computed (`c9c4a51`)
- T18.3: install in one line (`4109688`)
- T18.1: lock the 0.0.1 version (`4c3a251`)
- T18.4: codesigning and notarisation, written down (`be4da77`)
- T17.2: pin cargo-cache (`ce011cb`)
- T18.5: release-plz, the version as a pull request (`950fff3`)
- T18.5: anchor the release-PR check to a whole line (`173aeb2`)

### Plan
- plan: OpenAI API support (D11, phase P11) (`95f2f99`)
- plan: config file design (D12, phase P12) (`30f2be9`)
- plan: no third-party plugins (D6 rewritten, T0.8, P3/P6/P8 reworked) (`872090c`)
- plan: D13–D14, roadmap, ideas, and v0.2+ deferrals (`b7aef53`)
- plan: per-plugin design research (D15, phase P14) (`2729ef4`)
- plan: work only on main (D16) (`4887982`)
- plan: DRY helpers; implemented tasks leave plan.md (`a628dc7`)

### Docs
- docs: architecture, README, plugin authoring, example, done.md (`bc6709f`)
- docs: AGENTS.md — use low-cost models for simple jobs (`ed971d0`)
- docs: record P15 ratatui TUI as a v0.2 surface (`0620cb5`)
- docs: Hugo docs site on GitHub Pages (`fdcf0ac`)

### Tooling
- build: changelog generation with git-cliff (`57097fb`)
- ci: replace the Makefile with the justfile (`edc4441`)
- ci: make a clean checkout green (`c5b28ed`)
- ci: install rustfmt and clippy with the pinned Rust (`ac66d6a`)

### Other
- T12.3–T2.1: config validate, Diesel store, plugin plans, measure, hooks (`830e049`)
- Ignore accidental ~/.rtok/rtok.log in the working tree. (`00be9fb`)
- Ignore .reasonix; add justfile for the check gate (`fd45326`)
- Gate P8: doctor measures MCP servers with args/env (`a513d0b`)
- Gate P5: replay the archive policy over transcripts (`d98bb6f`)
- feat(site): add rtok branding and homepage metadata (`2e5cfb7`)
- P12: clap defaults from config (`7a46b18`)
- P13: hook call_io never archives (`5a10176`)
- P14: PLAN Targets quote roadmap gates (`6c8d31e`)
- P8: record graph gate as passed (`b33f369`)
- P0: close scaffold review as historical freeze (`e0efb70`)
- P1: save before-rtok stats baseline (`16d76e7`)
- P2: install additive Claude Code hooks (`4913462`)
- P8: graph v0.2 survey and proposed tasks T8.3–T8.8 (`f5f8e61`)
- P8b: promote graph v0.2 survey to tasks T8.3-T8.8 (`7b8959c`)
- P8b: record three of four gate clauses as passed (`9564f99`)
- P8c: plan the LadybugDB graph backend (D18) (`5e10bd4`)
- P16: plan OpenTelemetry export (D19) (`0efc0fb`)
- P8c: record the LadybugDB backend in roadmap and architecture (`8e9583d`)
- P17: PostToolUse in the latency test, p95 re-measured (`0f7f9f0`)
- P17: gate passed, p95 re-measured on a quiet machine (`0ddce1f`)
- P16: a 404 stream is not served, not an error (`3b0e1bd`)
- P16: Jaeger 2.11 and Grafana otel-lgtm verified, clause 3 half closed (`933ca2c`)
- P8d planned, config set validated, P16 status put back in its section (`c31abee`)
- migration.md: how rtok replaces lean-ctx, plugin by plugin (`ab464a5`)
- P16: backend clause moved to Gate P18 and ideas.md I-33 (`669b973`)
- P18: release without homebrew, tap has no secret (`1aaef41`)
- P18: install via ketch from the internet (`6c55b45`)
